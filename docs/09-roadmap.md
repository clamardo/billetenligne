# BilletEnLigne — Delivery Roadmap

**Status:** v2, rewritten against what actually shipped · **Date:** 2026-08-10

Sequencing principle, unchanged and now proven: **the long poles are commercial, not technical.** Telco merchant onboarding runs 4–12 weeks and an anchor operator contract runs longer. Engineering sequences around those, never behind them — which is why the pilot below still ships without a single PSP integration.

Per-feature state, updated on every push: **[`10-build-status.md`](10-build-status.md)**.

---

## Where we actually are

```
Search → Seat map → Hold → Sign in → Book → Pay → Ticket → Board
  ✅        ✅        ✅       ✅       ✅     ✅      ✅       ✅
```

**Phase 1 is functionally complete.** A traveller signs in with an emailed code, holds a seat, names their passengers, gets a payment code, walks into an agency, a vendor takes the cash at a real till, the ledger balances, a signed ticket is issued and queued for delivery, and a conductor scans it offline. An operator draws a seat layout, adds a coach, opens a route, publishes a timetable and prints a manifest — in a browser, with no curl anywhere.

Every step of that is executed by a test, most of them against real Postgres.

What remains before a pilot is **commercial, not technical**, which is what this roadmap said in its first line and is now literally true: an anchor operator has to sign, and somebody has to sit with them while they configure their fleet.

**Done:** the domain, the schema with its tenancy and ledger guarantees, the public sales boundary, the whole traveller API surface, email sign-in, booking and cash payment with double-entry postings, ticket issuing, the operator console's API, `services/worker`, the typed client, the Kilo component library, the traveller app through to a payment code, the standalone boarding scanner, and CI that executes all of it.

1,292 tests · 356 smoke checks · 38 executed schema guarantees · 417 further tests against real Postgres, and 10 against real Azurite.

(That total read 1,091 in an earlier revision. The number was wrong, not the suite: `dart test services/api` had been counted with `services/api/build` present, so `dart_frog build`'s copy of every package was counted a second time. `10-build-status.md` records the correction.)

---

## Phase 0 — Foundations · **complete**

Everything after this is faster because of it, and that turned out to be true in a way worth recording: the layer check, the schema guarantees and the smoke suite each caught a real defect during Phase 1 that review had already passed over.

- ✅ Monorepo, pub workspace, CI with the layer-boundary check (ADR-0001)
- ✅ `bel_domain` — money, market, policies, seat layouts, state machines
- ✅ `bel_localization` — YAML catalog, fr + en, with the drift guards
- ✅ `bel_contracts` — the wire format
- ✅ `bel_crypto` — Ed25519 and HMAC, against published vectors
- ✅ `bel_design` — Kilo tokens, three themes, fourteen components, the complete Material 3 `ColorScheme` and ~30 component themes, Inter + Fraunces bundled, sixteen components, eighteen pieces of artwork as editable SVG compiled into both the Flutter apps and the API, every empty and error state in the four apps drawn, a hero on the traveller's home screen and on the public storefront, a persisted dark-mode choice, and the operator's own hue on the ticket, the search row, the storefront and the boarding pass — one table of eight, asserted against the server's copy of it
- ✅ `bel_client` — the typed API client, shared by every surface
- ✅ Postgres schema, RLS, double-entry ledger, append-only audit
- ✅ **The public sales boundary** (ADR-0023) — not in the original plan, and the single largest thing this phase learned
- ✅ Dart Frog skeleton, auth and idempotency middleware
- ✅ `infra/dev` — Postgres, Firebase emulator, Azurite, Mailpit

**Not done, and deliberately deferred:** the component gallery app. Useful for review, not on the path to a sale.

---

## Phase 1 — Cash-only pilot · **in progress**

Ships without a PSP. The point is to prove inventory, ticketing, boarding and the console while telco paperwork is in flight.

### Done

- ✅ Search, seat map, hold, release — API and app, against real Postgres
- ✅ The traveller app's browse-and-hold funnel, fr + en
- ✅ Conductor mode: offline scan, five verdicts, standalone app
- ✅ **Identity — sign in with a one-time code** (ADR-0024). Email leads, not phone: Firebase phone OTP needs a real billed project and we have no provisioned SMS sender, so we run the challenge over a rail we control and answer a correct code with a Firebase *custom token*. This is the fallback ADR-0018 already documented. Phone is a config value away — the channel is a column, an enum and a switch, and the SMS template is already written.

### Also done since
- ✅ **The admin surface** (`/admin/v1`) — operator queue, operator page, approve · activate · request info · reject · suspend · reinstate, and the per-operator commission. `platform_staff` is finally read, which it never had been
- ✅ **Tickets and history in the app** — the QR, the live 30-second code, one ticket per seat, and unpaid reservations listed with the code to pay them. The receipt's "see my ticket" button now shows a ticket rather than returning to a search box
- ✅ **Cities from the server**, so the app holds no copy of an operator's network
- ✅ **Booking and cash payment** — reserve, collect, post the ledger, issue the ticket, all in one transaction
- ✅ **Ticket issuing** — Ed25519, under 300 bytes, inside the capture
- ✅ **The operator console's API** — fleet, routes, timetables and the materialisation the pilot was blocked on
- ✅ **`services/worker`** — the outbox drain that delivers a ticket, the payment poller, the sales horizon, and three sweepers that are deliberately not guarantees
- ✅ **The operator console** — Flutter web: fleet, routes, timetables, the dispatcher's day, the guichet, manifests
- ✅ **The vitrine** — title, tagline, accent from the closed set of eight and a header pattern, with a live preview drawn by the real widgets, plus the public storefront behind `blt.cg/o/<code>` — and now the logo, with the object storage it needed. What we accept is decided by the **bytes**, never by the header a caller sent, and it is a cap rather than a downscale: re-encoding somebody's brand mark is a silent change to the one asset they care about most, so 40 KB and 512 px are refused with the number to get under rather than resampled behind their back
- ✅ **The admin back office** — Flutter web: the review queue, one operator's file with its documents and trail, the six decisions, the negotiated commission, and the `indeterminate` reconciliation queue with its three exits. The stated reason lives in the frame and nothing writes without one
- ✅ **TOTP on both back-office surfaces** (ADR-0013) — RFC 6238, proved against the RFC's own Appendix B vectors before a line of the flow existed. Taken ahead of logo upload because it is a control on a live cross-tenant surface and needed no infrastructure that did not already exist. Three decisions worth restating: **travellers are never asked** (a second factor in front of a coach ticket protects one person's own bookings); **enrolment is not enforced by refusing to sign in** — staff with nothing enrolled get a session and land on the enrolment screen and nowhere else, because the alternative locked out every existing staff account the hour it shipped; and the half-session between the emailed code and the authenticator code is a **signed claim, not a row**. The flow lives in `bel_backoffice` so the console and the admin app cannot drift into asking differently
- ✅ **The seat-layout section builder** — the coach no preset fits, drawn section by section, with the traveller's own seat map redrawn on every keystroke. Three inputs that used to be **500s** are now field-named 400s: `abreast: "abc"` threw out of a capacity getter, `9+9` walked off the end of the seat-letter table, and a hundred-thousand-row section asked the server to build a hundred thousand labels before writing every one of them. The rule about what a seat arrangement may be now lives in `bel_domain` as a parser that returns null instead of throwing, so the console asks the same question the server does and disables the save button with a reason rather than enabling it into a refusal. The gesture half of `06-fleet-and-routes.md` §3.3 is now built too: a seat is taken out of service by touching it on the preview and given back by touching it again, doors and lavatories are placed by coordinate, and sections move forward and back without being retyped. Blocking **subtracts** rather than shortens — the seat keeps its label so the manifest and the conductor's count still match the physical coach, and no seat row is written for it, which is what stops somebody holding a seat that does not exist. Reordering renames every seat in both moved sections, so their blocks are dropped and the operator is told, because a block silently landing on somebody else's seat is worse than two taps lost. Undo and redo are built too, and coarse on purpose: a step is a *decision* — a seat condemned, a section moved, a door placed — not a keystroke, because a history that recorded every character would need forty taps to get back past a row count somebody retyped. Each step is a snapshot of the whole screen rather than an inverse operation, since the first inverse somebody forgets to write is the one that quietly corrupts a layout
- ✅ **Refund terms and cash refunds** (ADR-0015) — the policy engine has been built and tested since week one and had never refunded anybody. Now: a wizard where the operator answers questions and the **domain writes the sentences**, so the terms a traveller reads before paying are generated from the numbers the server executes; policies append-only **by grant**, with `(policy_id, version)` copied onto every booking at sale time; and the counter path — quote, approve, collect. A refund **moves a debt** rather than undoing a sale, so the share the policy retained stays credited to the operator where they earned it. The ticket voids at approval and the seat goes back on sale in the same transaction; the claim code is single-use because the state moves in the statement that reads it. Not built and named rather than implied: `source` — disbursement back down a mobile-money rail — is a different API with a separately funded float, so it stops at `approved` and posts the debt without moving money
- ✅ **Scheduled materialisation in the worker** — the sales horizon extends itself. A rolling twenty-one days of inventory, filled by a pass that enumerates every active pattern of every active operator under the worker's own platform scope and then materialises each one back under its tenant, so the pass can *see* across tenants and can still only *write* inside one. Idempotent on the key the console's button already relies on, which is what makes a half-finished run safe to simply run again. Two refusals are deliberate: a **suspended** operator gains no new inventory, because inventory that cannot be boarded is worse than none; and a batch that hits its limit says **`more due`** in the pass name rather than finishing quietly, because a scheduler running nightly against a hundred operators should see a backlog as a number, not as travellers who cannot book three weeks out. Still by hand and named rather than implied: **nothing invokes the worker on a timer yet** — that is a cron trigger in the deployment environment, not a line of Dart
- ✅ **Operator onboarding as a wizard** (§2.2) — the first row in `operators` no longer arrives by SQL. The shape that mattered: **the application *is* the operator**, in the early lifecycle states the spec already names, so the review queue, the audit trail and the six decisions all keep working rather than being rebuilt for a second entity. The applicant is a **member of the public** — a traveller session, no tenant, writing into the table that defines tenancy — so the boundary is column-level rather than a handler that remembers: INSERT pinned by policy to `application_draft`, UPDATE granted on four columns, and the one transition an applicant genuinely causes routed through a SECURITY DEFINER function whose body is one UPDATE and the audit row they have no grant to write. **Activation creates the `org_owner`**, which is the line that removes the phone call — before it, "approved" still meant somebody ran an INSERT by hand. The checklist is one function in `bel_domain`: the applicant's progress bar, the reviewer's list of gaps and the server's refusal to accept a half-filled submission are three readings of it. Not built and named rather than implied: **document photographs**. The scans belong in `kyb_documents`, the public role has no grant on it, and `verify_public.sql` refuses to let one be added — so what the wizard takes is the pair §3.3 enforces against, a number and an expiry, and a reviewer collects the images by asking. The agreement is an acceptance with a timestamp, not a countersigned PDF
- ✅ **A bound on how many codes one host may ask for**, and the switch that turns the phone channel on — the engineering half of Phase 1's last item. The per-destination cooldown is real and cannot see the shape that actually costs money: one host walking a list of a thousand addresses never triggers it once, and every one of those is an email or an SMS we pay for. Thirty per hour per source now, env-tunable, and **deliberately loose** — carrier-grade NAT means one address in this market is routinely one *building*, so a bound tight enough to stop a determined attacker would lock out an agency counter. It is a **cost control before it is a security control**, and saying so is what decides the number. Two details are load-bearing: the address is never stored, only an HMAC of it under the same key the codes are hashed with, so the table cannot become a log of who asked from where; and the source is the **rightmost** `X-Forwarded-For` hop rather than the leftmost, because the leftmost is whatever the caller felt like typing. Alongside it, `/public/v1/market` now announces which channels the deployment can actually deliver on and the traveller app renders the option from that announcement — so the day a sender number is provisioned is a config push rather than a release (ADR-0006), in a market where a large share of users never update. What remains is the number itself, which is a purchase
- ✅ **A demo world, and the command that removes it** (`infra/dev/seed/README.md`) — several things this product does only happen to a company that has existed for a while: paperwork three weeks from lapsing, an application small enough for the onboarding pass to approve on its own, a second operator on the same road willing to take stranded passengers. Until today every one of those paths was written and had never once been watched working, and the first sighting of the compliance banner would have been the week a real operator's insurance was about to lapse. Now there are five companies, a fortnight of departures and twenty paid passengers, and the states the product branches on are all present at once. **Built through the real adapters, not by inserting rows**: the wizard starts the applications, the platform console approves them with a reason that lands in the audit log, the operator console draws the layouts and the roads and materialises the departures, and the **guichet sells the seats for cash** — hold, reserve, capture, ledger, signed tickets, queued messages. That last part is what makes the world usable rather than merely present: most of what this product does only exists once somebody has paid, and a disruption with nobody to tell, a rescue coach with nobody to move and an open call on an empty departure are three screens that render and prove nothing. Tomorrow's 06:00 carries twelve people and is the coach to break. Cash rather than a rail, because a rail sale has to be authorised on a handset nobody is holding. A seed that INSERTed its way to a finished world would prove that INSERT works — and would quietly skip the ledger the payout run later reads; this one fails if the product does — which is also why two of the five states are produced by the worker rather than by the seed, so *run the pass and watch it happen* is the demonstration rather than a screenshot. **The mark is the delete path.** Every operator carries a `DEMO-` code and every person a reserved address, and `--purge` removes exactly those — because seeded data whose removal is untested is seeded data still sitting in production a year from now, under a name nobody recognises, with a real ticket sold against it. The removal **order is read out of the foreign keys at runtime** rather than written down: most tenant tables cascade from `operators`, but the ones that hold money — bookings, tickets, ledger entries, payouts, the audit log — are RESTRICT on purpose, and a hand-written list of those would be correct today and quietly wrong the first migration later. The one table the walk cannot see is the outbox, which holds no foreign key to anything because a message must outlive the row it describes long enough to be sent (ADR-0019), so it is named explicitly and the suite counts leftovers rather than trusting the walk. The seeder needs a **superuser connection** the product does not have, and that is the correct shape rather than an inconvenience: creating people and appointing platform staff are writes no surface holds a grant for (0012), and `audit_log` refuses DELETE by grant *and* by trigger (0004), for the table's owner too. The purge **suspends that guarantee rather than breaking it** — `DISABLE TRIGGER` takes an ACCESS EXCLUSIVE lock, so no other session can write an audit row while it is off, and Postgres DDL is transactional, so a purge that fails halfway rolls the trigger back on along with everything else. Unmaking a world is not an operation the product should be able to perform, and having to say so out loud is the correct cost. The city catalogue is deliberately left behind, because two operators serving Pointe-Noire must mean the same Pointe-Noire. And the screening adapter that finally makes auto-approval approve somebody is **keyed to the operator's own `DEMO-` code, not to an environment flag**, so `BEL__SCREENING=demo` surviving into a real deployment auto-approves nobody — the same reasoning as a payment rail shipped without credentials, one line further on. 13 Postgres tests, which seed, exercise both worker passes, check the tickets are signed and the ledger balances, put out a real call for help on a coach with real people on it, seed a second time to prove re-running replaces rather than stacks, and then purge and count what is left

### Remaining, in dependency order

**Nothing.** Every engineering item on this phase's list is built. What is left is commercial: an ACS sender number for SMS, telco merchant onboarding, and an anchor operator's signature.

The unbuilt engineering has moved to Phase 2, where it belongs: the re-accommodation plan and payout runs — both since built, along with the `config/markets.yaml` loader.

**Stated, not hidden.** The **phone channel has no sender number**, so the API answers 503 for it and the market announces `email` alone. Everything behind it — the challenge, the template, the drain, the app's own control — is built and switched off by config rather than by a missing code path. An application collects **no document photographs** — the numbers and the expiry dates, which is what §3.3 actually enforces against, and a reviewer asks for the images. That is a schema guarantee rather than a shortcut: the public role has no grant on `kyb_documents` and `verify_public.sql` refuses to let one be added. The worker's nightly run is a **cron trigger that does not exist yet** — the horizon pass is built, tested and idempotent, and until something invokes it on a schedule the far edge of the sales window moves when somebody runs the worker. A refund to `source` — back down the rail it was paid on — posts the debt and stops there; only the cash counter path completes, which is what the anchor operator actually needs and is the honest half to have built first. The section builder has **no undo/redo** — blocking a seat is one tap to reverse and a fitting one button, so the case that has no way back is the reorder that discards the blocks in the two moved sections, and it now says so on screen rather than doing it quietly (`06-fleet-and-routes.md` §3.3). There is **no QR code** on the TOTP enrolment screen — a QR encoder is a few hundred lines of Reed–Solomon and this repository has no independent decoder to check one against, so a bug would ship as a code that scans cleanly and produces a factor that never matches; the setup key is typed instead, which every authenticator app accepts. The TOTP **seed is not encrypted at rest**: a KMS key living in the same environment as the database is reassurance rather than a control. The storefront at `blt.cg/o/<code>` is now a page rather than only JSON, and what it still lands on is not: **`/` is the API's JSON stub**, so the "voir les départs" link on every line and the follower page's own call to action both point at an address the traveller web build has yet to fill.

**Exit:** the anchor operator sells real seats through our console for real cash, and conductors board with our scanner. *Revenue: zero. Learning: maximum.*

Everything that exit requires is now built. What is left is a signature.

**In parallel, from now:** Airtel and MTN merchant paperwork, the Orange Money conversation, the anchor operator LOI. **This is the actual critical path and no amount of engineering shortens it.**

---

## Phase 2 — Mobile money · **built, awaiting credentials**

The engineering is done. What is missing is a merchant agreement, which is the long pole this roadmap has said it was from the first line.

- ✅ Payment orchestration and the intent state machine (`indeterminate` is a first-class state, with a queue, a worker pass and a screen that does not call it a failure)
- ✅ **Airtel Money and MTN MoMo adapters**, both against the real APIs. Independent, so whichever set of credentials lands first ships first
- ✅ The in-app experience end to end: choose a wallet, name the number to debit (**not necessarily your own**), confirm where the money is going, watch for the PIN prompt, receipt
- ✅ Operator collection accounts in the console, saved unverified because mobile money has no chargeback
- ✅ The poller, because callbacks get lost — that is a fact about these networks, not a hypothetical
- ✅ **Commission netted at source, at the rate each operator negotiated** — a term of one contract, read from their row when the fare settles, in basis points. Not a market rate and not a constant: the number a large carrier argues for is not the one a two-coach family business gets
- ✅ The full failure taxonomy, each case with its own copy and its own recovery
- ⬜ **Production credentials.** MTN and Airtel run against sandbox hosts; the card and Orange Money adapters run against no host at all — there is neither a merchant account nor a merchant key for this market — and a fake rail covers development. Commercial rather than engineering: every one of the four is a contract and a set of secrets, and none of them is a line of code
- ✅ **The `indeterminate` reconciliation console — before launch, not after the first incident.** The queue is joined to the booking, the operator and a number to call, with three exits (ask the rail again · captured · failed) and the actor and reason written to the append-only event log. It is a screen in the back office now, and the two terminal exits refuse to fire without a sentence about what was actually seen
- ✅ **Disruption, declared and told** (`08-disruption.md` §1–§4) — the dispatcher declares one of six kinds and everything downstream is derived: the departure's new status, the exemption on every affected booking, one message per passenger, in their own language, from the outbox rather than inline with a roadside request. All of it in **one transaction**, because bookings marked involuntary with no declaration behind them is a refund entitlement nobody can account for. Three properties are load-bearing: a disruption is **public** — the follower of a shared trip link holds no account and is exactly the person who otherwise phones the agency; it is **not editable afterwards**, by a column-level grant rather than a promise, because it is the operator's evidence in a dispute; and there is **one open disruption per departure**, by a partial unique index, so "what is happening to my coach right now?" has exactly one answer. A **short delay entitles nobody to anything** — an hour is the line, it lives in the domain, and the console asks it rather than restating it, so a dispatcher sees what their declaration will cost before they confirm it. The form is built for a roadside on 2G: four large targets, a cause, and a new time chosen in offsets
- ✅ **Disruption — the rescue coach** (`08-disruption.md` §2.2 option ①) — the first thing an operator here actually does about a breakdown: send the spare. The bookings do not move, the passengers keep their journey, and the seats are **remapped by the domain** onto whatever the new coach has — a passenger keeps their label only when the target has one of the same kind, because `1D` is a window on a 2+2 and the middle of the back block on a 2+3, and handing them the same number would be handing them a worse seat while telling them nothing changed. Every ticket is **re-signed in the same transaction as the new manifest**, since the QR carries the seat (ADR-0007); a swap that left the tickets alone would have the scanner admitting somebody to a seat the manifest has given away. A coach that cannot seat everybody is refused **with the number short**, not with a "no" — "9 short" tells a dispatcher which coach to look for next. Holds with nothing behind them are released rather than slid under somebody mid-checkout
- ✅ **Disruption — the rebooking wave** (`08-disruption.md` §2.2 option ②, §2.4) — no spare coach, so the passengers go on the operator's own next departure. **Every replacement seat is taken before a single old one is released**: the transaction makes it atomic either way, but the ordering is what keeps a paid passenger from existing without a seat on any coach at all, including in the middle of a raise. Both departures are locked in id order, because two dispatchers moving people between the same pair of coaches in opposite directions is a deadlock on exactly the morning it happens. **Partial coverage is a success and is said as a number** — the 14:00 has eighteen seats and forty-two people need one, and "18 / 42" is what a dispatcher acts on; refusing anything short of everybody would mean the tool only works on the days it is not needed. A party moves **whole**, in **booking order**, because splitting a family to make the arithmetic work is not something anybody accepts at a counter and booking order is the only rule that can be said out loud to whoever is left. The fare each passenger paid crosses unchanged, even onto a dearer departure (ADR-0016)
- ✅ **Disruption — the protection agreement** (`08-disruption.md` §5) — the commercial half of option ③, which is the half that has to exist first. A dispatcher whose coach fails already walks the forecourt looking for a competitor with room; it settles in cash, at the roadside, with an argument about what a seat was worth. The agreement is that handshake written down once, in an office, by the people whose job it is: which roads, at what discount off the rescuer's public fare, up to how many seats a month, one way or both. **One party writes the terms and the other accepts them** — the payout run's rule one size down, because an agreement one party could switch on alone would be an invoice one party could write alone — and after acceptance the discount, the ceiling and the roads are frozen by a column-level grant rather than by a handler. This is also the **only row in the schema that belongs to two tenants**: an agreement neither party can read is not an agreement, so the policy names exactly two operators and an executed guarantee proves it names no third. The ceiling reads `31 / 40 places ce mois` **on the card rather than on the refusal**, because a dispatcher planning a rescue needs to know the agreement is nearly spent while there is still time to find another one. The rebill is shown as money on a real fare — "− 15 % · 7 650 FCFA sur un billet à 9 000" — never as basis points, and the settlement posts one operator's payable against the other's with **no commission and no cash**, so it nets into the next payout run
- ✅ **Disruption — the protection movement** (`08-disruption.md` §2.2 option ③, §2.3, §5) — the agreement now moves people. A dispatcher whose coach has failed picks a competitor's departure from the **public search — the same list any traveller sees** — narrowed to the companies an agreement covers, later, with room; the ask lands on that company's console with the four facts §2.3 says a receiving operator needs to answer: who, how many, on which of their coaches, and what they will be paid. **Accepting applies the movement in the same transaction**, because a request accepted now and applied later is a window in which the receiving operator sells the seats they have just promised, and the person who finds out is a passenger at a door. The new seats are taken **before a single old one is released**, both departures are locked in id order, the bookings change hands — `operator_id` and `departure_id` together — and **every ticket is re-signed under the receiving operator's code**, since the QR carries the operator as well as the seat (ADR-0007) and a ticket still naming the broken company scans as somebody else's passenger. The rebill is the agreed discount on the **rescuer's** fare, not on what the stranded passenger paid, and it posts one payable against the other with **no commission and no cash** — taxing a rescue would discourage the only behaviour the agreement exists to encourage. This is the **one operation in the system that crosses a tenant boundary**: neither operator's connection can see both halves, so the privilege escalates into a single narrow transaction that re-checks the agreement is still active and the request still pending — an agreement suspended between the ask and the answer authorises nothing. Two SECURITY DEFINER functions were needed rather than a widened policy, and they are the interesting part: a competitor's `departures` row carries their pricing, so what crosses is when the coach leaves, whether it is on sale and how many seats are free — every one of them already visible to any traveller with the app. **Partial coverage is a success and is said as a number, twice** — before the decision on the card and after it in the notice — because "2 of 5" is what sends somebody looking for the other three. Nothing about the ask pretends to be a placement: the sheet says so next to the button and the notice says so again, since a dispatcher who reads *envoyé* as *placed* stops looking for a coach
- ✅ **Disruption — the passenger's own choice** (`08-disruption.md` §3.2 option ⑤) — the passengers choose between the plans themselves, which §3.2 argues produces better coverage than any dispatcher plan: a released seat returns to the pool other affected passengers are drawing from, so every person who opts for the 14:00 makes the rescue coach fit somebody else. **A default is always already assigned** and the screen says so — nobody is left holding nothing while they decide, because a passenger who closes the app mid-thought must still be on a coach. Choice here is an upgrade on a safe state, never a prerequisite for one. **Every travel row states the arrival time**, since that is the question actually being asked at a roadside; a screen that answers "when does it leave?" makes somebody do the arithmetic standing up. **The refund is last, always present, never behind a support conversation** — and it is issued as an **agency claim with a code**, not as a promise to push money back down a rail, because a mobile-money disbursement we cannot make is a worse answer than a counter somebody can walk into. The **deadline states its own fallback** in the same sentence, because ambiguity at 04:00 is worse than a rule somebody dislikes. Two boundaries are deliberate. **Another company's coach is never offered to a passenger**: protection is an operator-to-operator settlement under an agreement, and a traveller who picks a competitor's departure would be committing two companies to money neither of them agreed to — so the alternatives are the operator's own, later, with room, inside a 36-hour horizon. And **the option list is never cached**: seat counts are the entire content of this screen, the window is re-checked before anything moves, and the departure is locked before the seat is taken, so a screen that has been open for ten minutes cannot move somebody onto a coach that filled — it refuses, re-reads and shows what is left **above** the options rather than instead of them
- ✅ **The ledger in anger: payout runs** (`04-payments.md` §6.2) — prepare, approve, release, and the gap between them is the control. **An operator cannot create, approve or edit their own payout**: `bel_app` holds SELECT on `payout_runs` and nothing else, so the party being paid cannot move the row that pays them — a privilege rather than a handler, which is what makes it hold against code written next year. **The person who prepared a run cannot approve it**: two people, not two roles, because one super-admin pressing both buttons is a formality and this is the largest movement of money we make. The amount is the ledger's own balance — `payable:operator:<id>` less their tills, read again at release — never the sum of the statement's lines, because a period sum drifts from the books the first time anything lands a day late. Releasing debits the payable and credits **every till plus the bank in one movement**, which is what makes "cash sales never generate a payout" true in the ledger and not only on the statement. A week of nothing but cash comes out negative — the operator owes us the service fees — and is refused as a transfer rather than posted with a minus sign. **The back office works the queue** — the whole statement is in the row, because somebody approving a number should be able to check it without navigating anywhere — and an operator reads their own statements in the console, with the cash line and the sentence that answers the question they ask first. And the statement is now a **document**, not only a screen: one page of A4, written by a hundred-line PDF writer rather than a layout engine, in the two standard fonts every reader has had since 1993 and with the figures in Courier so a money column can be right-aligned by counting characters instead of by carrying a font-metrics table. Uncompressed on purpose — a statement is three kilobytes and being able to read it with `strings` when somebody disputes what we sent is worth more than the bytes. **The commission rate on it is derived from these sales**, not read from the operator's row, because the row can be renegotiated tomorrow and a document that reprints today's rate over last month's money is the kind of small dishonesty that ends a relationship. **Nothing is invented**: §6.2's mock shows change fees and dispute adjustments, neither exists in the ledger, and a `0 FCFA` row for something we never compute would be a more convincing lie than leaving it out. The console downloads it through the authenticated client and hands it to the browser — a plain link sends no bearer token — and the back office serves **the same bytes from its own route**, so a reviewer approving a number and the company being paid are never looking at two different prints. What is still missing is **emailing** it: attachments are not built on the ACS adapter, and a statement that arrives as a link to a login is not the thing §6.2 asks for
- ✅ **The `config/markets.yaml` loader** (ADR-0006) — the file existed, was written down carefully, and was read by nothing: the API answered `/public/v1/market` from a constant compiled into the binary, so switching on Orange Money — a rail already in the file, one `enabled:` away — needed a release. Now the file is the authority and `Market.congoBrazzaville` is the **fallback**, which is the right way round: absent is a normal state (a fresh clone, a unit test) and the compiled-in market keeps the API answering, but present-and-wrong is somebody's deliberate push and **kills the process before it is healthy** rather than quietly serving last release's rails under a green deploy. The dialling table moved with it, so a **renumbered carrier prefix** is the same config push — the number a subscriber signs in with, the wallet the payment screen pre-selects, and the operator's settlement account are all parsed against the file rather than a constant. A currency we do not know the exponent of is refused by name and never guessed, because the exponent is what decides whether 9 000 is nine thousand or ninety. The two definitions are **checked against each other in a test**, which immediately earned its keep: the compiled-in fallback was offering a card rail the file had switched off

**Exit:** a traveller pays with Airtel Money and boards. Payment success ≥ 88% first attempt.

The *payment path* is built end to end and waiting on a telco. The phase is not: the reconciliation console exists, disruption is declared, told and recorded, a rescue coach can be sent, the passengers of a broken coach can be moved onto the next departure, a passenger whose coach failed can be carried by another company under an agreement signed months earlier, that passenger can now make the choice themselves rather than have it made for them, and money leaves under two-person control, with the statement downloadable as the document an accountant files and emailed the moment the run is released and a rail enabled by pushing a file — but **production payment credentials** are a telco's decision rather than ours, and **emailing the statement** waits on attachment support in the ACS adapter. Saying "everything is built" here — as an earlier draft of this section did — is exactly the kind of claim this document exists to refuse.

---

## Phase 3 — Public launch

- ✅ **The file somebody installs** — per-ABI release APKs for agency sideloading, built and interrogated by `tool/release_android.sh`. **Not published**: there is no Play Console account, so the `.aab` is built and goes nowhere, and the assetlinks fingerprint the script prints has nothing to be pasted into yet. Both are commercial rather than engineering. The launcher icon is the brand mark
- 🔨 **iOS release** — the project is checked from a machine that cannot build it (`dart run tool/check_ios.dart`, 16 checks in CI), which found five defects including a boarding scanner with no `NSCameraUsageDescription` — a process iOS terminates the instant the camera opens, with no dialog and no log. **Nothing has been compiled**: no Mac, no Apple Developer account, no provisioning profile, no signed build
- ✅ **Trip sharing and the follower page** (ADR-0014) — the person waiting at Pointe-Noire at midnight is the one who phones the agency, and they hold no account. So the traveller mints a link and sends it into whatever conversation they were already having; whoever opens it sees the coach. **The page is HTML the API serves, not the Flutter app** (ADR-0004): the follower is somebody on a borrowed handset with 2G who will open it once, and asking them for a 3 MB canvas download to read one progress bar would be answering a different question. Six kilobytes, self-contained, rendered before it fetches anything — the route, the times and the operator's name are in the first response — then it polls once a minute for movement. **The link follows a coach, never a person**: no seat, no booking reference, no fare, no name, no telephone number crosses to a follower, and that is a property of the SECURITY DEFINER function's OUT columns rather than of the handler that calls it, checked by an executed guarantee that scans the signature itself. **A disruption reaches the follower**, which is the whole point — the one moment the person at the station most needs to know is the one the passenger is least able to explain. Progress is honest about **which tier it came from** (§2), and there are now two of them: the conductor's handset confirms passage at a waypoint, so the page says *passé Dolisie à 10:42* on a solid bar — and falls back to a dashed one saying *estimation d'après l'horaire* the moment nobody has confirmed anything, rather than drawing a confident line somebody would take for a position. The token is 160 bits, **stored only as a SHA-256 hash**, so a database dump is not a set of live links; it dies **six hours after arrival** on its own; and it can be revoked in one tap, with the opens count on the sheet beside it — which tells somebody their message arrived, and tells somebody who sent it to the wrong group that it did too, while there is still time. Revoked, expired and never-issued all answer identically, because a page that distinguishes them is an enumeration oracle. **Opening the share sheet mints nothing**: a traveller who taps *partager* to see what it does must not discover afterwards that they published their journey. And **platform staff cannot create one** — the insert policy names the purchaser, so a support agent who can read everything still cannot publish a customer's trip
- ✅ **Cancel, by the traveller** (`01-feature-spec.md` §8.2) — the half of "modify & cancel" that carries the money, and the half most ticketing systems make you phone for. **The commonest cancellation in the system is a reservation nobody paid for**, and the screen calls that a *release* rather than a refund: a payment code that was never used owes nobody anything, and offering somebody a claim code for nought francs reads as a bug to the person being told it. For a paid booking the sheet quotes what comes back **beside what is kept** — a traveller who sees only the smaller number assumes a mistake, one who sees both is reading a policy they agreed to — under **the terms the booking was sold under**, resolved by the join to `(policy_id, version)` rather than by the operator's current default (ADR-0015 rule 1). The quote and the money come from the same `quoteRefund` (ADR-0004), so "the app said 8 100" cannot become an argument. **Cash paid at a counter comes back at a counter** whatever the policy's destination field says, because a journey paid in notes never had a source to send anything back to; a wallet policy writes the debt as `approved` and the screen commits to a **window** — *sous 72 heures* — and never says *envoyé*, since the disbursement float is not built and promising an arrival we cannot make is the one thing worse than a wait. **A policy that gives nothing back is a warning, not a closed door**: somebody who knows they cannot travel would rather free the seat than no-show, and the sentence says it in words rather than showing `0 FCFA` beside a confirm button. The seat is **on sale again inside the same transaction** — a seat freed at 22:00 the night before is a seat somebody else buys, and that is the whole commercial argument for building this rather than answering the phone. Two boundaries are new: a **payment still in flight refuses the cancellation**, because releasing the seats a second before a capture lands is the one outcome neither end can undo; and `refund_policies` is now **readable by the public role** — the first widening of that boundary since it was drawn — because the terms are printed before purchase by §4.1 and quoted at cancellation by §8.2, with every write still refused and an executed guarantee asserting both halves
- ✅ **Reschedule, by the traveller** (`01-feature-spec.md` §8.1, ADR-0012 D-08) — the other half of §8, and the half the spec is most emphatic about: **the fare difference and any fee are on every result row before selection**, not on the row you tapped. That is not decoration — somebody scanning a list is comparing prices, and a screen that prices one row at a time makes them tap five times on a connection that drops. D-08's three numbers finally exist as data: free with at least so much notice, a percentage of the fare inside a window, refused inside a cutoff — stored on the same row as the refund terms and stamped onto the booking by the same `(id, version)` pair, so "the terms it was sold under" covers changes too without a second versioning scheme to keep honest. **The new seats are taken before a single old one is released**, both departures locked in id order, and **the ticket is re-signed in the same transaction** because the QR carries the seat and the departure (ADR-0007). Three boundaries are deliberate. **A cheaper departure gives nothing back**, and the screen says so above the list rather than after the tap — refunding a downward difference means a disbursement we cannot make or a counter claim worth less than the counter time it consumes. **A row that cannot be taken is shown with its reason** rather than dropped, because a departure missing from a list is a departure somebody telephones an agency about. And **a change that owes money is refused, to the franc**: the difference has to be settled before somebody boards a coach they have not paid for, collecting it here needs a payment intent bound to a held-but-unapplied change, and until that exists the amount is stated and the traveller settles it at a counter — which is how these agencies already work. An **operator-caused change is free inside every cutoff there is** (ADR-0016), checked before the window rather than after it. The three numbers are the operator's own: they are answered in the refund-terms wizard, on the same save and the same version as the refund bands, because a booking carries one `(policy_id, version)` stamp and a second screen would mean two versions for one commercial decision — with a cutoff later than the free window refused by the button, by the route and by a CHECK constraint, since no single field could catch it
- ✅ **Collecting the difference in the app** (`01-feature-spec.md` §8.1) — the boundary the reschedule slice named, closed. A change that owes money is no longer sent to a counter: it becomes a **change order**, which holds the seats on the target departure, states what is owed to the franc, and **moves nothing at all**. The booking moves when the capture lands, and only then — a change applied on `pending` is the free journey an optimistically issued ticket would be (ADR-0005), one departure further along. The order is an ordinary `holds` row plus a promise beside it, which is what makes the expiry story free: the sweeper that already puts lapsed holds back on sale puts these back too, and a second pass only records that the promise attached to them has lapsed. One live order per booking, enforced by a partial unique index, because a traveller tapping two departures while the first prompt is still on their handset would otherwise hold two sets of seats and owe two amounts. Changing their mind before paying releases the first order; **a prompt already in flight refuses the second**, since releasing those seats could strand money that is about to land. The payment funnel is the existing one — same rails, same PIN, same poll, because from the rail's side it is the same transaction — and the intent carries a change id that decides, at settlement, whether a capture confirms a booking or moves one. **A client names which debt and never how much**: the amount is read from the order's row inside the transaction that opens the intent, and the row policy that lets a traveller open it checks that the order is theirs, still waiting, and not yet lapsed. A capture that arrives after the seats went back on sale **moves nobody** — it closes the order and leaves an intent a human can see and refund, which is the honest outcome and the reason the hold outlives the payment window
- ✅ **An order the traveller can cancel before it lapses** (`01-feature-spec.md` §8.1) — the last named gap in §8. A change that owes money holds seats for a quarter of an hour while the traveller pays, and somebody who backs out of the PIN prompt was returned to the priced list with **nothing anywhere saying so**: the only ways out were to pay it or to wait it out, and neither is a thing anybody would guess. The order is on that screen now — what is held, what is owed, when it runs out — with two buttons and no third, because "change to a different departure instead" is the list underneath and spelling it out would be describing scrolling. **Cancelling is a different verb from abandoning, deliberately**: backing out of a payment leaves the order alone, since people come straight back and those seats are theirs for the window they were promised, while cancelling is the traveller saying they no longer want the change at all and the seats go back on sale at once — the same argument that made releasing a hold worth building rather than leaving to the sweeper, since on a coach that is nearly full a quarter of an hour is a real sale. A **payment in flight refuses it**, with the same refusal reserving a second order gets and for the same reason; **nothing waiting is a 404 rather than a cheerful 204**, because the sweeper may have got there first and a traveller told "done" about seats they no longer hold will believe it
- ✅ **Card via PSP, for the diaspora** (`04-payments.md`, ADR-0005, ADR-0006) — the rail an uncle in Paris pays a nephew's fare on, and the first one that **leaves the app**. Every rail until now pushed a prompt to a handset: the traveller types a PIN into a menu we do not control and the answer arrives by callback or by poll. A card does not work like that, and pretending it did is what would have broken it — so the port asks the rail `pushesToHandset` rather than switching on its id, and every branch that follows falls out of that one question. **The card number never touches this system**: the traveller is sent to the processor's own hosted page, which moves the entire PCI surface to the PSP, and there is no version of "just this once" that is worth taking it back. There is also **no operator collection account**, because there is no wallet at either end — the money lands in the processor's merchant account and reaches the operator through the ordinary payout run, so the option carries no number, the screen says what it does instead of showing a merchant account that does not exist, and the one query that decides whether a rail may be offered had to learn the difference without loosening anything for wallets. The URL is **stored, not held in memory**: the app may be killed while somebody is typing a card number into a browser, and the screen they come back to offers the same page rather than minting a second transaction for one journey — which is how a traveller gets charged twice. The return URL is a **hint and never authority**: anybody can type one, so coming back confirms nothing and the capture is settled by re-querying like every other rail (ADR-0005 rule 4). Two CHECK constraints keep the two shapes honest — a push rail has a wallet, a checkout rail has a page, and neither can borrow the other's column. **No merchant account exists for this market yet**, so the adapter is written against the shape every hosted checkout in the region shares rather than one vendor's field names, and a sandbox rail in the same file — deliberately, so the two cannot drift — mints pages that go nowhere and charge nobody. Switching it on is `CARD__APIKEY` plus `enabled: true` in `config/markets.yaml`: both halves, so a deployment with credentials still does not offer a rail the market file has not announced. 6 unit · 5 Postgres · 8 traveller-app · 4 contract · 10 smoke · 2 schema guarantees
- ✅ **Operator reliability, on the row where the choice is made** (`08-disruption.md` §6) — the contracts have carried `onTimeRate` since the search results were first drawn and nothing ever computed one. Now a nightly worker pass does: of the coaches that actually ran in the last ninety days, how many ran without the company having to tell anybody something had gone wrong. **Not a departure-time comparison** — we hold no departure timestamps, and inventing a threshold on data we do not have would produce a confident number that means nothing; what this figure says is a fact the operator controls and passengers care about. **It is a column, not a query**: search is the hot path, and a join over ninety days of departures on every search on 2G is how a results screen becomes slow for a figure that moves once a day. **Below twenty departures there is no figure at all**, and a missing figure draws nothing — an operator with three departures and one breakdown is not "67 % on time", they are an operator nobody knows about yet, and a 0 % chip beside a new company is a judgement we have no data for
- ✅ **Stations become real** (`06-fleet-and-routes.md`) — the `stations` table has existed since the first migration and nothing had ever read it: a traveller saw "Brazzaville → Pointe-Noire" and was told nothing about which of an operator's yards to stand in at half past five in the morning, which is the fact an agency's telephone line repeats more than any other. Now a departure names the terminal it leaves from and the one it arrives at, the console has a catalogue on the same screen as the roads — because they are the same decision, a company opening Dolisie has to say which gate before the first ticket prints — and the name plus the operator's own directions reach the search row and the ticket. Four things are deliberate. **A station belongs to exactly one operator**, and `operator_id` stopped being nullable: the tenant policy reads `operator_id = app_tenant_id()`, which is false for NULL, so the shared *gare routière* the column seemed to allow was invisible to every company sharing it — a shared yard is one row per company, exactly as the desks, the staff scoping and the tills already are. **A coach cannot be said to leave from a rival's terminal**, by composite foreign key onto `(id, operator_id)` rather than by a WHERE clause somebody forgets, with an executed guarantee to prove it. **The terminal is captured onto the departure**, like the seat layout: renaming or closing a yard next month must not rewrite where a coach that is already sold was said to leave from. And **a closed yard is deactivated, never deleted** — the operator keeps reading it, the public surface stops offering it, and last month's passengers keep the address they were given. The results row names the yard **only when the list offers a choice of yards**: one terminal per city is the normal case, and printing the same line on eleven rows teaches somebody to stop reading the line that matters on the day one coach leaves from somewhere else. Not built and named rather than implied: the **coach's registration is still not on the traveller's ticket** — it lives on `vehicles`, which the public role has no grant on at all, and copying it onto the departure would drift the first time a dispatcher swaps in a rescue coach
- ✅ **The passenger who missed their coach** (`06-fleet-and-routes.md`, ADR-0012 D-08) — the transfer the stations slice made tellable. Today a missed coach is a dead ticket and the system says so twice before anybody speaks to a human: self-service change offers only departures that have not left, and D-08's cutoff has already refused everybody inside two hours of departure. Somebody standing at the terminal at 06h05 for the 06:00 has been refused by two screens, and what happens next is that they plead with an agent who puts them on the 09:30 or does not, according to nothing written down. Now it is written down: **two numbers on the refund policy** — how long a missed ticket keeps any value, and what the transfer costs as a share of the fare paid — stamped onto a booking by the same `(id, version)` pair as the refund bands, so a late passenger is judged by the terms they were sold under (ADR-0015 rule 1). **Both default to zero, which means not offered**, and unlike the change terms there is no ADR default to inherit and none was invented: honouring a missed ticket is a commercial promise, and a platform that defaulted it to "yes" would be making that promise on every company's behalf. **The list crosses routes, not companies** — candidates are matched on the city pair rather than the route id, which is the whole reason the stations slice came first, since a company's two Brazzaville terminals are two rows in `routes` and the 09:30 from Kinsoundi is invisible to any query keyed on the road somebody was sold. Another company is never offered: that is a new purchase, not a transfer, and it would move a fare between two operators' books. Every row names its yard, and a yard that is **not** the one they were told to come to is drawn in the warning colour, because it is a taxi ride somebody has to be told about. **It is a counter screen and deliberately not in the app**: the person is in front of an agent with a printed ticket, the decision is the company's to take in person, and the fee is cash into a drawer somebody counts at the end of a shift — so the quote is re-taken under the lock, and a paid transfer must name its till, refused by CHECK constraint as well as by handler, while a free one names none
- ✅ **The funnel, from the rows the sales already left** (`04-payments.md` §8) — where people leave, on a screen in the back office, derived from holds, bookings and payment intents rather than from a tracker. Nothing new is recorded: the funnel and the ledger are counted from the same rows, so the tickets on this screen are the tickets in the accounts, and there is no second pipeline to keep honest. **It is emphatic about what it cannot see.** A search leaves no row anywhere, so the first figure is a seat held, and the note above the table says so — a funnel whose top step is unlabelled gets read as search-to-ticket, and then somebody reprices a route from a number that never measured price. Counting search means a telemetry path with its own consent question, and that is a decision to take deliberately rather than by accident. Three properties do the work. **The cohort is the day the seat was held**, not the day the money landed: somebody who holds at 23h50 and pays at 00h10 is one journey, and bucketing on the payment would invent a failure on Monday and a conversion from nothing on Tuesday. **Quiet days are rows of zeroes, not missing rows** — the alert §8 asks for is a day-over-day fall, and a fortnight with the Sundays absent compares Monday against Saturday. And **nought out of nought has no percentage at all**: a day nobody held a seat draws "rien ce jour-là", because a 0 % chip is an outage somebody investigates. The ten-point drop is computed from the same list the screen draws and shown on it, since the person who can tell a broken rail from a broken funnel is the one reading it. Gated on `finance.read` — no traveller, no takings, no telephone number is on it — and still an audited read, because it crosses every tenant and `11-security.md` asks that crossing to leave a trace whether or not what it read was personal
- ✅ **Offline: the ticket that survives being killed** — the app had always kept the last list it loaded **in memory**, which survives a lost connection but not a killed process, and the moment that matters is a passenger boarding at six in the morning somewhere with no signal on a handset that swapped the app out overnight. A QR that needs a request to render is a QR that fails exactly where it is needed. The stored list is now drawn **before** the request goes out rather than after it fails — on a dead connection the difference between the two is a spinner for however long a timeout takes — and it arrives marked stale, on the banner the list already had. **Hand-written SQL against `sqlite3` rather than Drift**: there is one table and three statements here, a code generator plus a build step is a large thing to carry for that, and it would be the only codegen in a repository that writes raw SQL against Postgres everywhere else. What is stored is **the booking as the server sent it**, JSON and all, because a set of columns mirroring the DTO is a second definition of the wire format that drifts the first time a field is added — and a ticket is only useful whole, since a row missing its signature renders a QR no conductor will accept. **Keyed by traveller and replaced rather than merged**: a handset here is shared, and the list the server answered with is the whole truth about what somebody holds, so merging would keep a refunded ticket renderable forever. A cache that cannot be read degrades to "nothing cached" and never to a crash on launch
- ✅ **The session that survives the app being killed** (ADR-0013) — `BelSession` has taken a `SessionStore` since the day it was written and every surface handed it `MemorySessionStore`, which is why a traveller signed in again every launch and a conductor signed in again in a yard at half past five with a queue behind them. The adapter now exists: `bel_secure_store`, the iOS **Keychain** and Keystore-backed **`EncryptedSharedPreferences`** on Android. **One package rather than a copy per app**, for the reason the second-factor screen is one widget — two implementations of one security control is two chances for one of them to be wrong, and the wrong one is the one nobody looks at again. Two decisions in it are the whole slice. First, **after the first unlock rather than on every unlock**: the app is launched at 04:30 by somebody who has not looked at their phone yet, and a token the Keychain withholds until the next unlock is a traveller signing in again at a coach door. Second, **every failure is "not signed in", never a crash** — the Android Keystore genuinely loses keys after a restored backup or a fingerprint reset, and the standard symptom is a decrypt error on read; an app that throws there is one that can never start again on that handset, so the token is forgotten and the sign-in screen is shown, which is a nuisance rather than a brick. An unwritable store does not fail a sign-in either: the session is in memory for this launch either way, and what is lost is only its surviving a restart. **The two web surfaces deliberately do not use it.** The same package has a web implementation and it is not secure storage — it puts an AES key in `localStorage` next to the value it encrypts, which is obfuscation wearing the word *secure*. The honest equivalent there is a same-site cookie set by the server, a slice of its own, so a console session still ends when the tab closes and that is written down rather than dressed up. 5 tests, against a scripted platform channel — a test host has no Keychain, so what is asserted is the half that is ours: which options we ask for, and what happens when the platform says no
- ✅ **The ticket you can always get to** (ADR-0026) — a ticket is worth what the person holding it can reach at the coach door, and today there are two ways to reach one and both assume our app on their handset: the traveller app's own list, and a plain-text message that says *your ticket is in the app* — no QR, no link, nothing a conductor can scan. That is fine for somebody who bought on their own phone. It is not fine for the person this market is full of: **the walk-in at an agency counter**. The guichet already does the structurally right thing — a counter sale creates an unverified account from the number the vendor types, so the booking has an owner and agency sales reconcile against digital ones — but that account has no verified channel and, with SMS off in this market until a sender number exists, **no way to be reached at all**. The ticket is issued, signed, and unreachable except on paper. So: **one link**, `blt.cg/b/{token}`, claimed by the app and served as a page when the app is not there. **The token is the credential for seeing and nothing else is** — no name-and-date-of-birth gate, because those are guessable and shoulder-surfable and cost a real customer more than an attacker; the defence is 160 bits, single-purpose, expiring, revocable and stored only as a hash, exactly as `trip_shares` already proves. **Seeing and changing are different acts**: changing a seat or moving money takes a one-time code to the channel the ticket was delivered on, which is sign-in rather than a second auth system — and which **claims the booking**, turning the counter's unverified account into one the traveller holds. That is the highest-value line in the feature: it is how a walk-in becomes an app user without anybody selling them anything. **The static QR is the printed ticket's equal, and is written down as such**: rotation defends against a ticket forwarded in advance, and the control at the door is one scan per seat, which the scanner already enforces. The counter offers only channels the market can actually send on, and the page offers only what the booking's rails can finish — a cash sale names the agency rather than showing a refund button that ends in an apology. **Landed: the link exists.** `ticket_links` is `trip_shares`' shape with two differences that come from what this link is for — it lives until the coach has arrived plus a day, because a boarding pass has to survive a coach that left four hours late, and it **remembers the address it was sent to**, so the step-up code can only ever go where the ticket already went rather than to one the caller supplies. One live link per booking per channel, by partial unique index: re-sending revokes and re-mints, because two live links are two things to revoke and the traveller was only told about one. **The plaintext token exists in the message and nowhere else** — the counter's POST returns 202 and an address to read back, not a URL, since the drain mints the token inside the transaction that composes the message (ADR-0019), and a link on a till screen is a ticket the next person in the queue can photograph. The reader is a SECURITY DEFINER function taking a hash, never a policy: a SELECT policy on this table would be row-enumerable, which is a list of every live ticket in the country — proved by an executed guarantee that an anonymous session counts zero rows on the table and still opens its own ticket through the function. A revoked link, an expired one and a token nobody ever issued are **one 404**, so a dead link says nothing about whether it was ever real. **And the page exists.** `blt.cg/b/{token}` is rendered whole on the server, **QR included**, with no JavaScript at all: the follower page renders a shell and fetches, because it is watched for ten minutes; a boarding pass is looked at once, at the worst moment, and a second round trip there is a second chance to fail. The QR is inline SVG rather than an `<img>` at an endpoint, for the same reason — a request that fails after the page has painted is somebody holding a blank white square up to a conductor — and it is **black on white in both themes**, because a dark-theme QR is one the cheap scanner at the door refuses. `/b/` and not `/t/`: a follower link deliberately shows no seat, no price and no name, and this one is the seat and the name; one prefix serving both is how a follower link eventually renders a boarding pass. **A voided seat is shown and struck through, never dropped** — a family of three whose middle ticket was refunded must not find a page with two seats on it and no explanation. The yard is on it, with the company's own directions, which needed a second migration: Postgres refuses to change a function's return type in place, and that refusal is exactly the point — what the holder of a link may know is a decision taken in SQL, not a line somebody edits in a handler. A roadside stop that names no yard prints none, rather than the terminal in Brazzaville the coach left from. And the page says, once and while they still have signal, that the honest fix for no signal is the message in their inbox. **And the code is in the inbox.** The page is what they open; the message is what they still have when they cannot open anything, so the QR travels as a **file attached to the email**, one per live seat, named `BEL-4KQ2M9-12A.png` so three codes in one inbox are three findable codes. An `<img>` pointing back at us is an image that vanishes on a plane or in a village with no signal — which is the reader this whole feature exists for — and Gmail strips `data:` URIs while Outlook renders SVG as nothing at all. So a PNG, written here rather than pulled in: one-bit greyscale, no filtering, and a zlib stream of *stored* blocks, which is legal deflate every decoder reads and sixty lines rather than a compression library in the API for one attachment. The test decodes it with a real zlib decoder and compares every pixel against the encoder's grid, because a QR drawn one module wrong is not one that looks wrong — it is one that scans as something else. **A voided ticket is not attached at all**: an image in somebody's inbox that boards nothing is worse than no image, because they find out at the door. The HTML body carries the road, the company, the hour, the yard and its directions, the reference and every passenger — **in addition to the plain text and never instead of it**, which in this market is most of the handsets — and the drain's message became leg-aware on the way past: a passenger who bought Dolisie→Pointe-Noire is now told Dolisie, at the hour the coach reaches it, and sent to the yard there rather than the gate in Brazzaville the coach left from. **And the vendor is asked.** The question lands on the receipt — the dialog that appears the instant cash is taken — because that is the only moment the customer is standing in front of the vendor and can spell their own address. Two channels and one field, and the field is **allowed to be empty**: blank means the address already on the account, which for a counter sale is the number the vendor typed to create it, and re-typing it is how a wrong one gets in twice. The notice **names the address rather than saying sent**, because the vendor's next sentence is reading it back, and a typo caught at the counter costs ten seconds while one caught at the coach door costs a ticket. Revoking appears only once something has been sent, next to the send-again button: that pair is the whole of what a vendor can do in ten seconds when a customer says they forwarded it to the wrong person. Every refusal is a fact about the world — not paid yet, nowhere to send it, SMS not available in this market — because the person reading it is standing at a counter and has to know what to say next. **And seeing is now separated from changing.** The token opens a ticket and deliberately nothing else; cancelling, refunding or moving a seat takes a one-time code, and **the caller supplies no address** — the code goes to the one frozen on the link at mint time, because an endpoint that took a destination would be an open relay with our domain on it. It is **sign-in rather than a second auth system**: the challenge, the sixty-second cooldown, the five-attempt cap and the per-host limit are ADR-0013's, the code is answered at the ordinary `/auth/sessions`, and the traveller ends the flow holding an ordinary session. Asking where to send **does not count as an open**, or the open tally would be a lie. Then the claim, which is the highest-value line in the feature and the least visible: the booking is re-pointed at the account that just proved it holds the address, so a walk-in becomes somebody with an account without anybody selling them anything. Two rules, both in SQL rather than in a handler — it is **idempotent**, because a traveller claiming from two devices is one person twice and not a conflict, and **a booking held by a verified account is never re-pointed**, because the guichet's unverified account is what this is for while an account somebody has actually signed in to belongs to a person. Both are executed as a schema guarantee rather than trusted, and the refusal is a 403 rather than a 404 on purpose: the token was good, the booking was not available to take. **And the link opens the app, where there is one.** `blt.cg/b/{token}` is one URL with two renderings — the app when it is installed, the page when it is not — and deliberately **no interstitial in between**, because an *install our app* screen in front of a boarding pass at a coach door is the worst screen we could build. Android's manifest claims `https://blt.cg/b/*` with `autoVerify`, so there is no *which app?* chooser, and `flutter_deeplinking_enabled` hands the URL in as the initial route rather than through a plugin channel — one less package, and the path arrives before the first frame. The domain's half of both handshakes is **served by the API rather than dropped in a bucket**: a static file on another host is one that goes missing the day somebody changes hosting, and it fails silently, months later, as *the link stopped opening the app*. Apple's is served unsigned, with no `.json` extension, which is what iOS has required since 9 and the usual way the file is got wrong. **Blank is a supported state**: with no release certificate the fingerprint list is empty, the file is still well-formed and still served, and Android simply declines to verify — the link opens the page, which is the correct fallback rather than a failure. `/t/` is claimed by neither platform: the follower page is opened by strangers with no account and no app. iOS is one Xcode change away — the entitlement file exists and declares `applinks:blt.cg`, and attaching it to the target needs a Mac and an Apple team, neither of which this deployment has. **And the app opens on the ticket.** Android hands the URL in as the initial route before the first frame, so the app launches straight onto the ticket list rather than onto search and then jumping. The token is **remembered rather than acted on**, because a walk-in opening their first link is not signed in yet and a claim needs an account to claim for — it is taken on the way into the list, which is exactly where somebody who has just signed in arrives. **Once, and never retried**: a link that cannot be claimed, because it was revoked while they signed in or the booking is already somebody's, is dropped rather than re-attempted on every visit, and it never stops the tickets they do have from loading. What they see afterwards is the app's own ticket — the rotating freshness code, the offline vault, the whole screen — rather than a second read-only one with a static QR, which is the point of claiming rather than rendering. 49 unit · 18 Postgres · 4 worker · 35 smoke · 2 schema guarantees
- Manual smoke on real SIMs, real money, real sunlight (ADR-0021 layer 5)

**Exit:** 55% search→ticket conversion · 99.5% crash-free · ≤ 2.5 s cold start on the 2 GB reference device.

---

## Phase 4 — Depth

- ✅ **Orange Money** (ADR-0005, ADR-0006) — ~45% of the market, and the rail that turned out **not to push**. MTN and Airtel take a number and ring a handset; Orange's Web Payment API takes an order and answers with a page, the traveller authorises it on Orange's own site, and we find out by asking. Everything that needs was built by the card slice a day earlier, so this rail cost **a class and an `if`** rather than a second payment funnel — which is the whole return on `pushesToHandset` having been a question asked of the rail rather than a list of ids. Three consequences, and each is a place the old assumption would have broken: it needs **no payer number** (the traveller types their own on Orange's page, and sending one would be a field Orange ignores and a wrong-number support call we invented), the **carrier check does not fire** (a rail we cannot push to cannot be pushed to the wrong carrier), and there is **no USSD fallback** on the waiting screen — a menu that knows nothing about this payment is worse than no menu. Unlike a card it **does have a merchant account**, so the option names who is being paid exactly as MTN's does; the screen now reads `hostedCheckout` off the option rather than off the rail's `kind`, because Orange Money is mobile money **and** a hosted checkout at the same time and no category can say that. `pay_token` is stored on the intent as the rail's own reference, because Orange requires it on every status query and it is not derivable from anything else we hold. The catalog moved with it: the checkout screen's words are `payment.checkout.*` and name the rail from the key the server sent, so the page no longer calls Orange Money somebody's bank. **Written against no merchant key** — there is no Orange contract yet — and if the eventual one turns out to be their merchant-*push* API instead, this adapter is the only file that changes. `config/markets.yaml` still ships it off: credentials and the market file are two halves of one switch. 2 unit · 5 smoke checks against a sandbox
- ✅ **Section-based layouts in the console for buses** — shipped in Phase 1 as the seat-layout section builder, and listed here unticked for longer than it deserved. The cabin-section model is validated on the low-risk case exactly as this line asked: an operator draws the coach no preset fits section by section, `KSeatMap` — the widget that actually sells the seat — redraws on every keystroke, and start rows are computed rather than typed. What air will need on top of it is a second class with a different fare, not a different model
- ✅ **Inter-operator protection at scale — calling the whole road** (`08-disruption.md` §5) — the two slices above need a counterparty who agreed in advance, and on the morning a coach fails at PK 84 the honest state of the world is usually that nobody did. An agreement is a relationship, and relationships take months; a breakdown takes ninety seconds. So option ③ now works with **nobody on the other end of it**: the dispatcher broadcasts, and every company that opted in and runs that road sees the call within a refresh. **The terms travel on the call.** The sender's own public fare per seat, no discount — a discount is what a relationship earns and there is no relationship here — stated up front rather than negotiated, because an operator deciding in ninety seconds whether to take forty-two strangers needs the number in front of them, and because a price that could move while a console had the call open is a price nobody would answer twice. The three columns a sender may write after the fact are `state`, `closed_at` and `answered_by_operator`; the seats and the money are not among them, by grant. **Who was invited is computed in a policy, not asserted by a handler** — opted in, active, still selling, and running this road *in this direction*. Each of those is one afternoon's convenience away from being dropped and each is executed in `verify_public.sql`; the direction clause is the difference between a broadcast and a bulletin board, and the "still selling" clause is the expiry ladder two slices up doing its work here for free, without anybody editing this table. Opting in is a **timestamp rather than a boolean**, because *since when* is the question a dispute asks and a boolean cannot answer it. **First to accept wins, and winning is the same commit that moves the passengers.** Two dispatchers reaching for the same forty-two people at 06:04 is the normal case, not the edge one: the election is a conditional UPDATE on the open row, the loser gets `protection.call_closed` and their own coach is untouched — and if the movement then turns out to move nobody, the election rolls back with it and the call stays open for whoever is next, since a call closed by a rescue that did not happen is the worst of both. A movement now names **exactly one authority**: `protection_requests.agreement_id` became nullable and gained `call_id`, so a client asks which one is present rather than reading a `kind` string free to disagree with the ids beside it. Two smaller decisions, both load-bearing. **The inbox is one payload** with `receiving` beside the rows, because *nobody needs help right now* and *you never opted in* are the same zero rows and completely different screens. And the capability split **runs the other way here** from every route beside it: broadcasting and answering are `disruption.declare`, the roadside decision, but joining the channel is `protection.manage` — agreeing to carry other companies' passengers is a standing commitment about what the company is for, and a dispatcher may call for help without being able to promise, on the company's behalf, that it will keep answering. The read policy is deliberately **not** restricted to live calls, so an operator can still read the call they answered last month and their own movement history keeps making sense; the cross-tenant read is a SECURITY DEFINER function returning only facts a traveller could already see, exactly as the queue two slices up does. A call with no end is a call on every console on the road next week, so the window is clamped server-side to between a quarter of an hour and half a day and a worker pass closes the ones nobody answered — **marked, never deleted**, because the coach that broke down and the fact that nobody came is what an operator reads when they decide whether this channel is worth being in. The demo world puts both selling companies in the channel, and does it **through the desk rather than by setting the column**, so the trail says which owner joined and on what date. 8 contract · 16 Postgres · 4 worker · 10 console · 2 demo · 18 smoke tests, and 1 schema guarantee
- ✅ **The scanner's wire (ADR-0022).** The boarding scanner has been a real app since Phase 1 — real signatures, real verdicts, a real offline decision — but pinned to a departure it **built locally**, which meant the one thing it could not do was board a coach anybody had sold a seat on. It can now, end to end. **Built: the manifest it pins.** `GET /console/v1/departures/{id}/boarding` answers with everything the door needs and nothing else: the passengers, the **per-ticket rotating secrets** that make a screenshot detectably stale, the ticket-signing **public keys**, and every ticket **voided since it was signed** — named rather than merely missing, because an Ed25519 signature stays valid forever and a device left to infer a refund from an absence boards somebody who already has their money back. It is a read of `tickets` rather than of bookings, since a ticket is what gets scanned. **The keys travel with it** instead of being compiled in: a key compiled into an APK cannot be rotated without a store release, and a store release is a week in a market where these handsets are updated over an agency's wifi at best — the trust anchor is the authenticated session that asked, which is already trusted with every passenger's name. It is behind `boarding.scan`, which the `conductor` role holds and holds alone: a response carrying the secrets that mint freshness codes must not be readable by a counter login. And it carries **no phone numbers**, unlike the dispatcher's manifest — somebody in an office rings a passenger who has not shown up, and a conductor's handset is the most easily lost device this company owns. **Built: and the redemptions come back.** `POST /console/v1/departures/{id}/redemptions` is where the device's outbox empties, whenever there is signal — in the yard at the far end, or that evening on the agency's wifi. **A batch, and idempotent**: a device that could not reach us retries the whole outbox and two conductors may work two doors of the same coach, so a repeat is dropped rather than allowed to overwrite the time somebody actually boarded — first scan wins, because that is the row a dispute is settled with. **The device's clock is what gets recorded**, not ours: it is the only clock that was at the door. The ticket is resolved and the boarding written in **one statement**, so a device confused about which departure it is pinned to cannot record a boarding against the wrong coach; the answer names what landed and what this coach has never heard of, and **both leave the outbox**, because a ticket that is not on this departure will not start being on it and a handset that retries forever is flat by eleven. **Built: the coach a conductor is looking for.** `GET /console/v1/boarding?date=` is a **second list rather than a filter on the dispatcher's board**, because the board is read under `booking.read` and the `conductor` role is `{boarding.scan}` and nothing else — widening that capability to reach one screen would have handed every conductor the whole day's load factors on every coach the company runs. So the row carries route, hour, yard and how many people are expected, and no money, no held seats and no passenger names. **Expected counts unvoided tickets**, so the row and the scanner's own counter agree before anybody scans anything, and the day is the market's local one rather than UTC's — the 06:00 belongs to the calendar day a conductor is standing in. A yard nobody named prints nothing, because a conductor sent to the wrong gate is worse off than one told nothing. **Built: and the device is on the other end of it.** The scanner signs a conductor in through `bel_backoffice`'s shared screen — the same one the console and the admin app use, **second factor and all**, because a conductor is operator staff and ADR-0013 does not have a *small* tier; weakening that to make one screen easier would have been a security control removed to save a tap. Then today's coaches, then one tap, then the door. **The pin is the last thing the network is needed for**: base64 secrets and base64 public keys become bytes, the keys arrive *with* the manifest so one can be rotated without a store release, and `pinnedAt` is stamped by the **device's own clock** — *how long ago did this handset last hear* is not a question a server clock can answer. **And the wire found a real bug the fixture had been hiding.** Ed25519 verification is asynchronous in every library worth using; the verdict is deliberately synchronous, because a conductor boarding sixty people cannot wait on a future per scan. The demo departure prepared every signature at the moment it *built* them, so the app looked right — but against a real coach the device has never seen a traveller's signature until the camera reads it, and every genuine ticket in the field would have come back **forged**. A `SignaturePreparer` port now does the async half on the one payload just decoded, immediately before the decision: one await between a camera frame and a verdict, and a test that fails without it. **The outbox empties when somebody asks**, never on a scan — the count sits in the header, the send is a tap, and both `recorded` and `unknown` leave the queue. A failed send changes nothing about who boarded, and leaving a coach with rows still queued asks first, because those rows live in memory and walking away is how they are lost. **The demo path goes through the same three calls**, so the picker, the pin and the drain are exercised on a fresh clone with no server at all. **And the queue survives the app being killed.** The redemption log was a `Map` in memory, which quietly broke the whole promise: Android kills a backgrounded camera app under memory pressure — precisely what a ten-minute boarding on a cheap handset produces — and forty boardings went with it, with nobody finding out until an operator asked why a full coach shows nobody on it. It is now one SQLite table on the handset, hand-written SQL like the traveller's ticket vault, and **first-scan-wins is the primary key** rather than a rule somebody remembered: `INSERT OR IGNORE` on `(departure_id, key)`, so a conductor's double-tap keeps the time a dispute is settled with. Scoped **per departure**, because a conductor works two runs in a day and the same 14A boards on both. A relaunch re-pins the coach and the counter comes back with it — `47 / 60` rather than `0 / 60`, which is the number they are watching. And a stranded queue no longer waits for somebody to happen to re-open that departure: **the day's list arriving is proof of a network**, so anything left over from an earlier run is sent right then, and named rather than sent silently. Opening the file is tried and never required — a handset that cannot is the app exactly as it was, working and unable to survive being killed. The session now survives too (see *the session that survives the app being killed*, below), so what is left in memory is the **device id** alone: `BEL_DEVICE_ID` names a provisioned handset, and without one it is unique per launch — enough to tell two doors of the same coach apart, not enough to recognise the same handset tomorrow. 6 contract · 6 Postgres · 3 client · 21 scanner · 18 smoke tests

- ✅ **A second company called *Trans* something gets a code, not a 500.** An operator's code is six letters of their name and three characters after it, generated once at signup — and on this road half the companies are called *Trans* something, so the three characters are the whole of the difference between two of them. The insert had no answer for a duplicate but to fail, which meant the second company to be named like the first met a 500 on the last screen of a wizard they had spent twenty minutes filling in. It is now **retried under a savepoint**, up to eight times, so the rest of the transaction — the application row, the applicant's own read — survives the collision that caused it. Found the way these are usually found: an unrelated suite went red once in forty runs on a duplicate key, which is the same event a real applicant would have met on a Tuesday. 1 Postgres test

- ✅ **Segment selling — Brazzaville→Dolisie on a Pointe-Noire coach.** A road can now be sold in pieces, end to end: priced by the operator, found by the search, drawn on the seat map, bought, printed on a ticket, listed for the conductor and read at the door. **Built: the road has stops.** `route_stops` had sat in migration 0001 since the first week with nothing writing to it, so a company that ran Brazzaville–Pointe-Noire through Dolisie had no way to say so and a traveller had no way to learn it. The console's route form now describes the whole road in one dialog — a road and the places on it are one decision, and a second screen would let somebody open a line and never say what it passes through. **Times are minutes from the departure, not from the previous stop**: that is the figure on the timetable a dispatcher already holds, and the other one is arithmetic they have to do before they can tell whether what they typed is right. **Ordering is a rule, not a presentation choice** — the list is sorted by time and numbered by the server, because a segment will be a pair of positions in that sequence and a list that numbers itself is a list that can be numbered wrong; two stops at the same minute are refused for the same reason. **Stops replace, they never merge**: a route form is a whole description of a road, so an empty list is how the last stop is removed, and omitting the field entirely leaves the road alone so a caller saving a duration cannot erase one. The **set-down-only flag** is the detail every naive model gets wrong and every operator notices immediately, so it is on the row rather than buried in the dialog, and a stop that serves nobody is refused outright — that is a place the coach slows down, not a stop. The traveller sees the towns on the search row, as codes the client resolves through the city catalogue it already holds (ADR-0008), computed by a correlated subquery rather than a join because a join to a one-to-many would have multiplied the seat count the same statement is producing. **Built: a seat's occupancy is a range** (ADR-0025). The obstacle was one column — `seats.state` is a scalar, and *sold Brazzaville→Dolisie and free Dolisie→Pointe-Noire* is one row with two answers. Occupancy now lives in its own table as a half-open range of stop positions, and **"two people cannot buy the same piece of the same seat" is an EXCLUDE constraint rather than a check in a handler**: it is a claim about concurrency, so it is made to Postgres, which refuses the second row on a Friday afternoon when a reviewer cannot. Half-open is the feature and not a detail — `[0,1)` and `[1,2)` share nothing, which is exactly a passenger alighting at Dolisie and another boarding there. **The whole road is a range too**, so an ordinary sale takes the same path as a segment; a design where the usual case bypasses the new machinery is one where the new machinery is untested in production. `seats.state` stays, stops being written by hand, becomes **derived by trigger**, and gains `partial` — so every reader that has not been taught about segments asks "is this available?" and correctly hears no, which is the only direction a change to inventory may fail in. The positions are **captured on the departure** rather than joined from the route, because a stop inserted next month must not renumber what was sold last month. And **a segment is sellable only when the operator has priced it**: no distance-based fraction of the through fare, because a fare is a commercial decision and an operator who finds we invented one will find it on the day a passenger paid it — which is also what makes this shippable, since until somebody prices a segment every departure sells its whole road for its whole fare exactly as it does today. **Built: the operator prices a piece of it.** The price list is the whole feature switch — the route form gains a table of legs, cities on the wire and positions in the database, and the server resolves one into the other because a position is an index into a road the client does not own. Which is also where the road's own rules get applied to a price: a set-down-only stop can end a leg and cannot start one, a leg running the coach's way round is refused by name, the whole road is refused because the timetable already prices it, and **a free seat is a mistake rather than a price** — caught where the person who typed it is standing rather than by the ledger three tables downstream. The currency is the market's and never the client's, exactly as a timetable fare has always worked, so a console that thought it was pricing in euros cannot put a euro row on a Congolese road. An empty list is how the last leg comes off sale and an omitted one leaves the list alone, so a caller saving a duration cannot take a road off sale by not mentioning it. **Built: every path that takes a seat now writes occupancy.** The claim, the release, both sweepers, the reservation and its capture, self-cancellation, the operator's refund, the passenger's own choice, the reschedule and its paid variant, the rescue coach and the inter-operator movement — twenty-two write sites across nine files, all of them now writing a piece of road rather than a state. **The grant is what makes it stick**: no application role holds `UPDATE` on `seats` any more, so a path added next year that reaches for the state directly fails in the test suite instead of holding a seat nobody can account for, and the one exception is a column — a traveller moving their own reservation's deadline. The seat race stops being a lock somebody remembered to take and becomes the exclusion constraint, which is a rule the database applies to paths nobody has written yet; `SELECT … FOR UPDATE` on `seats` is gone from all six places that took one, because the privilege it needed is exactly the one that was withdrawn. Three properties came out of doing it: a **sale re-attributes the hold's own row** rather than releasing and re-taking, so there is no instant in which a paid seat is free; an **unpaid reservation stays under its hold**, which is what makes it read as held rather than sold, and is what both the capture and the sweeper now look for; and **a claim that loses the race takes its own hold down with it**, thrown rather than returned, because a hold committed over seats it did not get would burn the idempotency key and answer the traveller's retry with "that key is spent". A seat map that meets a state it has never heard of now says **held** rather than available — a reader that fails open sells a seat somebody is sitting in. Departures also capture the road they were put on sale with at the moment they are created, so a stop added next month cannot renumber a journey already sold. **Built: the search finds a leg.** A traveller standing in Dolisie asking for Pointe-Noire used to get an empty screen while three coaches a day went past the door — the trade every operator on that corridor already makes at the roadside, out of a notebook. Now the same query answers with the coaches that pass through, and **the row that comes back is an ordinary departure between the two towns asked for**: the operator's price for that leg, the boarding time three hours after the coach left Brazzaville, the arrival at the stop rather than at the terminus, and only the towns in between. That shape is the decision — no new DTO, no second screen, and a client that has never heard of segments renders it correctly. The road is numbered inside the statement the way the departure's own span numbers it, the two endpoints added as rows because they are not stops, which is also where their asymmetry gets said out loud: **nobody alights where the coach starts and nobody boards where it finishes**, and a set-down-only stop can end a leg and never start one. Availability stops asking `seats.state` and asks occupancy, because on a leg the state is the wrong question — **a seat sold Brazzaville→Dolisie is exactly what somebody boarding at Dolisie should be offered**, and it reads `partial`, which every whole-road filter correctly refuses. An unpriced pair is simply not on sale, and withdrawing a price takes it off the same day. **Built: the seat map answers for that leg too.** The screen after the search row asks with the same pair the search was made with, so a position in a road the client does not own never crosses the wire — and the road's own two ends are simply the ordinary whole-journey request, which is what lets a client hand back whatever it searched with and never know which of the two it made. A seat sold Brazzaville→Dolisie is drawn as **free** to somebody boarding at Dolisie, which is the whole point and is invisible to anything asking `seats.state`. The price is the operator's price for the leg, flat across the coach: a section premium on a piece of a road is a commercial decision nobody has made, and inventing one is exactly what this ADR refuses to do with a fare. A pair nobody priced has **no seat map at all** rather than an empty one, because a coach drawn with every seat greyed out reads as full instead of as not-for-sale, and naming one end without the other is refused rather than quietly widened to the whole journey — that would price a leg at the through fare. **Built: and the traveller buys it.** The claim takes the same pair, resolves it against the operator's own price list inside the transaction — which positions, at what price, in which direction — and writes occupancy for that piece of road alone. **One seat, two travellers**: somebody riding to Dolisie and somebody boarding there hold the same seat at the same time, `[0,1)` and `[1,2)` sharing nothing, and a third asking for the whole road is refused because between them the two of them have all of it. A pair nobody priced is refused **by name** rather than quietly sold as the whole road at the through fare — nothing is wrong with the coach and retrying cannot help. **The hold remembers what it was quoted**, so the reservation charges that and a price list edited between the tap and the counter cannot rewrite what somebody agreed to pay; the booking remembers the piece of road, which is what a conductor's manifest will be read from once occupancy is gone. And the quote is the server's: `bel_public` held a table-wide `UPDATE` on `holds` from the first month, which the moment a price landed on that row became a way to buy a journey for one franc — the grant is now the one column it always was in practice, with an executed guarantee saying so. **Built: the conductor's list names the leg.** A manifest row now carries where that passenger gets on and off — which is the one place somebody finds out that 12A comes free at Dolisie, and the difference between a coach leaving that town half empty and selling the second half of the road twice over. **Only when it is a piece of the road**, though: the header already names the road the coach runs, so annotating every line with the two towns it already says would bury the one line that is different, and a list where everything is highlighted is a list nobody reads the highlights on. It is drawn from the booking's own span rather than from occupancy, because occupancy is released when a ticket is refunded and a manifest is a record of who is on the coach — the two disagree on exactly the rows a dispute is about. **Built: the ticket names the journey somebody bought.** A ticket that says *Brazzaville → Pointe-Noire, 06:00* to somebody who boards at Dolisie at nine is a ticket they cannot use to find their coach, and the one they end up arguing with a conductor about at the roadside. The booking's own span is now what the ticket is drawn from: the two towns of the leg, the hour the coach reaches the first of them — **minutes from the departure, which is how the operator typed the road, so a delay moves the boarding time along with it** — and the terminus arrival unchanged, because a passenger riding to the end is on the coach until it gets there. The **yard is the stop's own**, and a stop with no station named prints nothing rather than the terminal in Brazzaville the coach happened to leave from: saying nothing is the honest answer, and printing the wrong yard is how somebody misses a coach that was forty minutes away by taxi. A whole-road booking reads exactly as it always did — the commonest sale takes the new path, which is the only way the new path is ever tested. **Built: the door knows too.** The scanner's verdict now says *Descend à DOL* under the name, and the manual list — the one used when a phone is flat — carries the leg beside the reference. **The leg rides on the manifest, not in the QR**: the payload is signed, so putting it inside would mean a format change and every scanner nobody has updated refusing every ticket issued after the day we shipped it, for a fact the device already downloaded before the coach left the yard. It is on the refusals as well as the acceptances, because the verdict a conductor argues with is the one that says no. A whole-road ticket says nothing extra — a line that is always there is a line nobody reads. That closes the loop end to end: an operator prices a leg, a traveller in Dolisie finds the coach, buys a piece of the seat somebody else is riding to Dolisie in, holds a ticket that says Dolisie at the hour the coach reaches it, and both the printed list and the phone at the door know where they get off. The leg now reaches the door over the wire (ADR-0022) and through the demo manifest on a clone with no server, by the same mapping either way. 30 domain · 14 contract · 35 Postgres · 9 console · 4 traveller · 5 scanner · 28 smoke tests, and 4 schema guarantees
- ✅ **Self-serve onboarding for low-risk operators, without a human reviewer** — the review queue is worked oldest-first against a published 48-hour SLA (`03-operator-lifecycle.md` §2.3), and an SLA is a promise about the *tail*. The tail is mostly three-coach companies whose application is complete, whose settlement account name matches their legal name, and about which a reviewer has nothing whatever to add — and every one of those a person reads is a person not reading the fourteen-coach one whose name already exists in the table. So the small complete ones approve themselves, and everything else arrives **pre-sorted with its reasons already written down**. Two things make that defensible rather than reckless, and neither of them is trust. **The bar is exposure**: five coaches, six daily departures, so the worst case an automatic approval can produce is a bus-load rather than a season. And **the expiry ladder is the safety net** — an operator approved here whose insurance lapses is blocked from selling the day it does, by a pass with no human in it either, which is the slice immediately above this one and the reason this one could be built at all. **Bands, not a score.** A number out of five invites an argument about the threshold and hides which check failed; three bands with named reason codes say what would have to change. `elevated` — a screening hit, a duplicate of a company we already know, a previous offboarding — is **never auto-approvable at any size**, and it still carries the ordinary reasons alongside, because a reviewer opening that file needs the whole picture rather than the headline. **A licence that is valid but not for long is a review, not an approval**: ninety days of runway is required, so an automatically approved operator does not meet their first enforcement in week nine. **Assessment and approval are separate, and the assessment is written first** — including for the approvals — because an approval whose grounds were never recorded is an approval nobody can explain afterwards, which is the one outcome worse than a slow queue. The audit row carries `actor_type = 'system'` and a **NULL actor**: nobody decided, and inventing a staff id to satisfy a column would make the trail lie. A reviewer who reached the file first **wins the race** — the activation is conditional on `under_review`, so a person's decision is never overwritten by a pass. And the whole path is **switched off by data rather than by dead code**: there is no sanctions-screening contract, so the screening port answers `notRun`, `notRun` is not `clear`, and every application today still falls to a person while the sorting runs. The day a vendor exists it is one adapter and no other change — the same shape as a payment rail shipped without credentials. Two smaller things came with it: the band lands on `operators` rather than on `operator_applications`, because 0015 gives the applicant a blanket UPDATE on the application row and a risk band stored there is a risk band the applicant can set to `low` — `verify_public.sql` now executes that refusal for both the applicant and the operator — and the reviewer's queue names the reasons rather than counting them, because *2 signaux* is a row that tells you to open the file, which is the click the whole sort exists to save. 14 domain · 3 contract · 8 use-case · 9 Postgres · 1 worker · 3 widget tests, and 1 schema guarantee
- ✅ **Document expiry enforces itself** (`03-operator-lifecycle.md` §3.3) — `kyb_documents.expires_at` had existed since migration 0001 and nothing had ever read it except a count on the admin list, which means the state of the product until today was: an insurance certificate lapses in March, the company keeps selling seats in April, and the first anybody finds out is a claim after a crash. **Selling seats on an uninsured coach is a liability we will not carry**, so the calendar now enforces itself. The ladder — T−60 notice, T−30 notice and a console banner, T−7 a reminder every day and an SMS to the owner, T−0 no new sales, T+7 suspension — lives in **one domain object**, and that is the decision the rest falls out of: a worker computing `interval '30 days'` in SQL while a console computes thirty days in Dart is two answers to one question, and the day they disagree an operator is reading *il vous reste 30 jours* on a screen that has already stopped their sales. **Graduated is the whole point.** By the time selling stops, the company has had a notice at two months, another at one, one every day of the final week and a message to the person who can actually renew a certificate — the owner, not whoever uploaded it, because a reminder to a dispatcher is a reminder nobody acts on. Nobody is surprised, which is the difference between enforcement and an outage. **The latest copy of each kind wins**: an operator that renews its insurance every year has a row per year, and the lapsed 2024 certificate is history rather than a reason to stop selling — a rule that had to move out of SQL after the first version filtered *documents* by the sixty-day window and made a renewal invisible, leaving last year's lapsed copy looking like the company's standing. **Blocked is not suspended**, and they are two columns rather than one state because they undo differently: a block is a switch the pass flips back by itself the moment a renewed certificate is verified, and a suspension has a reinstatement procedure and a named human attached (§4). **What a block does not do**: cancel a departure, void a ticket, or stop the scanner. Everything already sold runs — §3.3's 72-hour grace is simply what falls out of blocking the *sale* rather than the *departure* — and the traveller-facing consequence is that a blocked operator vanishes from search rather than selling a seat and refusing it at checkout. An **unverified upload is a claim, not a licence**: it neither reassures the company nor blocks them, because whether a certificate is real is a reviewer's question and not the calendar's. Reminders are deduped by the outbox's unique key rather than by a `reminded_at` column, so the pass can run every ten minutes and still send one message a day; the final week's key carries the date, which is what makes *daily* mean daily. And the slice closed a hole that predated it: `bel_app` held a blanket `GRANT UPDATE ON ALL TABLES` from migration 0004, which on `operators` meant a console session could write its own `status`, its own `commission_bps` and — from today — its own `sales_blocked_at`. RLS cannot help there, since the tenant policy's whole job is to let a company write its own row, so the column list became the control exactly as it already is for the applicant role. **A block the blocked party can lift is not a block**, and `verify_public.sql` now executes that sentence. Both readers of the calendar are built: the operator's banner, which starts at thirty days and is behind no capability at all — a banner only the director can see is a banner that appears the week after they went on leave — and our own **Conformité** screen (§6), which is a calendar rather than a queue and is worked in the fortnight *before* the deadline, because after it the worker has already acted. 15 domain · 3 contract · 11 Postgres · 8 worker · 8 widget · 11 smoke tests, and 1 schema guarantee
- ✅ **Sold-out alerts** (`01-feature-spec.md`) — the 06:00 to Pointe-Noire sells out, and a traveller looking at *complet* has exactly one move: come back and look again, which most people do once and then buy from somebody else. Meanwhile seats **do** come free on that coach all day — a hold lapses, a reservation goes unpaid, somebody cancels — and nobody who wanted one was ever told. Now they are. The design decision that everything else falls out of is what an alert **is not**: it holds nothing, reserves nothing, and gives nobody a position. A queue over inventory that is simultaneously on sale to the whole market is a promise this system cannot keep at a coach door, so everybody waiting is told at the same moment and the first to pay gets the seat — and the wording is built around saying so out loud, because the natural reading of *nous vous préviendrons* is *vous avez une place*, and that reading ends with somebody at a station at half past five for a coach that filled overnight. **Asking twice is asking once**, by partial unique index rather than by a check in a handler: two taps on a connection that dropped are the ordinary case here, and two rows would be two messages. **It fires once**, with `notified_at` as the entire state machine, stamped in the same statement that queues the message — a seat that came free and went again is not news worth a second SMS, and an alert that fires twice is an alert people mute. **The party size is asked for, not assumed**: a family of four told about one free seat has been sent to a terminal for nothing, so the pass compares the free count against what each person actually needs and passes over the ones it cannot help. **A coach with room refuses the alert**, with the number, because the honest answer to "tell me when there is room" on a coach with room is "go and book it". A lapsed hold **counts as a free seat**, exactly as the claim path already believes, so an alert cannot sit waiting on a coach that is in fact sellable. Withdrawing is an UPDATE and never a DELETE — *I asked and was never told* is a support conversation a deleted row cannot answer — and it is silent when there was nothing waiting, because somebody who taps *ne plus me prévenir* twice got what they wanted both times. **The operator cannot see who is waiting**: the row policy names the traveller and nobody else, since a list of people who want a seat on the 06:00 is a list an operator could be asked to sell. The pass runs **after the hold sweeper**, and that ordering is the pass — an abandoned checkout is the commonest way a seat comes back, and it is not free until those seats have been released. A second pass closes alerts that can no longer come true, cancelled rather than notified, because nothing was sent. 9 flow · 4 contract · 11 Postgres · 6 worker · 4 widget · 7 smoke · 1 schema guarantee
- ✅ **Rebooking from past trips** (`01-feature-spec.md`) — the other half of the line above, and the cheapest slice in this phase because everything it needs already exists. A trip already taken is the best evidence there is about where somebody goes, and until now the app threw it away: a traveller who came back from Pointe-Noire last week and wants to go again re-typed two cities, a party size and a date into a form that had already been told all of it. **The return leg is offered first**, because an outward journey is nearly always a journey back and that is the one that costs the most typing. **Tomorrow, not today**: somebody opening a past ticket in the evening and shown today's board is shown coaches that have already left, which reads as a broken app rather than as a late hour — and tomorrow is what the search screen itself defaults to, so the two agree rather than disagreeing by one screen. **The party comes from the booking**, not from the last search: a traveller who searched for one seat and travelled with three is a traveller who travels with three, capped at what the product sells so an old booking made under a looser cap cannot build a query the server refuses. It lands **on the results rather than on a pre-filled form**, because one tap is the whole feature, and backing out lands on the form already carrying the road and the party with the date as the one thing left to change. One thing had to be corrected on the way: the demo gateway was putting *city names* on a booking where the server puts **codes** — `routes.origin_city` references `cities(code)` — so a "go again" built from a demo booking would have searched for a city called `Pointe-Noire` that no route names. The codes were also being printed straight onto the tickets list, which was asking a traveller to read a database; the list resolves them through the city catalogue the search screen already holds. 5 flow · 3 widget tests, and no server change at all
- ✅ **A refund goes back the way it came** (ADR-0015 rule 5, the other half) — `refunds.disbursement_intent_id` had sat in migration 0003 since the first week with nothing writing to it: a `source` refund was approved, the debt was posted, and then the row waited at `approved` forever while every screen told the traveller to walk into an agency. That was honest while there was no way to push money out; it is not honest now. The worker's `refunds` pass opens a **disbursement intent** — the same table, the same state machine, the same raw event log, plus a `direction` column — and asks MTN Disbursements or Airtel's disbursement resource to transfer. Four decisions carry it. **Asking is not paying**: the ledger is posted when the rail says the money left, never when it is asked to send it, because a rail can accept a transfer and fail it a minute later and a float that drained on requests would show money leaving that never did. **A refusal becomes a counter, not a dead row**: a barred wallet, an empty float, or twenty-four hours of silence turns into a claim code and an SMS, which is exactly where this product was before the rail existed — the traveller is owed the money either way, and a `failed` row is a debt on our books with nobody told. **The wallet is copied onto the refund at approval**, never read when the money is sent, so a traveller who changes handsets on Thursday cannot redirect Tuesday's approved refund — the same argument `payout_runs.destination` makes. **The rail's idempotency key is the refund id**, so a retryable pass cannot pay somebody twice out of a float a person has to fund. What stays a counter and always will: a ticket paid in notes, a card whose PAN this system has never seen, and **Orange Money, whose Web Payment product has no disbursement API at all** — named here rather than left to be discovered. Placeholder credentials throughout; the fake rail settles on the *second* ask rather than the first, which is what makes the asking-is-not-paying test able to fail. 2 domain · 7 worker tests, one migration
- ✅ **The second-factor seed stops being readable from a copy of the database** (ADR-0013) — a TOTP seed is the one secret here the server has to be able to *read*: it recomputes a six-digit code from it every thirty seconds, so hashing it — the answer everywhere else in this system — is not available. It was therefore stored in the clear, with an argument attached: a key living in the same environment as the database is reassurance rather than a control. Half right, and the wrong half was load-bearing. It is true against somebody who has the environment, and that is not how data usually leaves: a backup on a laptop, a restore into staging, a read-only injection, a disk sold with a filesystem still on it are each **the data without the key**, and separating those two events is the whole control. Seeds are now sealed with AES-256-GCM, self-describing as `v1.<nonce>.<ciphertext+tag>`. **An absent key is supported and loud** — a local stack stores them in the clear and the API says so at startup, because a random per-process key here would lock every member of staff out of both back offices rather than costing a code, which is what makes this unlike `AUTH_CODE_KEY`. **A sealed row that will not open is an error, never a missing factor**: a factor that quietly reads as absent is a factor an attacker can enrol again. **Rows written before the key are upgraded by being read**, in the transaction that reads them, so a deploy needs no backfill and nobody is locked out by one. The column stopped being called `secret_base32` in the same migration, because a column promising base32 while holding ciphertext is the kind of small lie that costs somebody an hour at the wrong moment. 6 crypto · 4 Postgres tests, one migration, one schema guarantee
- ✅ **The statement arrives, rather than waiting to be fetched** (`04-payments.md` §6.2) — §6.2 asks for a statement that is downloadable **and** emailed, and only the first half was built: an operator learned they had been paid by signing into a console and going looking. That is the wrong way round. Money left under two-person control and nobody outside this building was told. Releasing a run now writes a `payout.released` event in the **same transaction** that moves the ledger, and the drain renders the same PDF the two download routes serve and sends it as a file. Three decisions: **the event is written with the movement, not after it** — a run that released and failed to notify is a bug, a notification for a run that did not release is a lie, and one transaction is the only thing that rules out both; **it is email only**, because a statement is a page of figures and an SMS saying *check the console* is the link back to a login this slice exists to avoid; and **it goes to the operator's longest-standing accepted owner**, one address rather than everybody with a role, because a payout figure circulated to every dispatcher is a disclosure nobody asked for. The PDF is rendered in the worker from the run as it was released, so a later renegotiation cannot change the document already sent. 1 test against real Postgres
- ✅ **The bar stops guessing: the conductor confirms where the coach is** (ADR-0014 §1, tier 2) — the follower page has drawn a **dashed** bar since the day it shipped, and said so in as many words: *estimation d'après l'horaire*. It drew one because nothing could tell it otherwise. All three tracking tiers were written and tested in the domain, the page renders all three, and the missing piece was never a model — it was somewhere for an observation to come from. The obvious answer was a driver-facing app with background location, which is a permission dialogue, a battery argument and a product nobody asked for. The actual answer was **the handset that is already on the coach**: the conductor's scanner is pinned to this departure, trusted to say who boarded it, and in the vehicle. One more tap says *we have passed Dolisie*, and the person waiting at Pointe-Noire reads *passé Dolisie à 10:42* instead of an estimate. A waypoint is a **route stop**, not a coordinate — a place with a name people use, already carrying the minutes into the run, so the bar's fraction comes from the timetable rather than a number an operator would have to maintain and would get wrong the first time a road was rerouted. The tap **queues on the device** exactly like a boarding, because the RN1 is four hours with no usable signal and a confirmation that needed a network would be one nobody could ever make; it goes up in the same window, in its own request, and both the accepted and the unheard-of leave the queue. The **device's clock** is what is recorded — ours only as *when we heard* — because a checkpoint stamped with the hour it happened to sync reports the coach an hour behind itself. **First tap wins**, by primary key on the server and by `INSERT OR IGNORE` on the handset, so an offline device behaves the way the server eventually will. And the row is **append-only**: no UPDATE grant, no DELETE grant, and an executed guarantee that fails if anybody adds one, because a claim about where a coach was is evidence in a delay dispute and there is no undo on the screen either — a confirmation is a fact somebody may already have left for the station on. Still not built: **tier 1**, conductor GPS, which is the one that genuinely needs a surface nobody has asked for
- ✅ **Mailpit stops being decorative, and the local stack starts on a first try.** `infra/dev` has run a mail catcher since the beginning and nothing had ever written to it: a sign-in code went to the API's stdout, an operator statement went to stdout with `(+1: releve.pdf)` where the PDF should have been, and the one thing worth *looking at* — the email a customer receives — could not be looked at. There is now an SMTP sender behind the same notification port ACS sits behind, chosen only when no ACS connection string is configured, so a deployment with real credentials can never be diverted into a mail catcher by a stray variable. SMS is deliberately **not** mailed anywhere: there is no such thing as SMS over SMTP, and mailing the text would be a channel that looks delivered and is not. A mail catcher that is not running falls back to the log rather than failing the send, because a stopped container is a fact about somebody's laptop and not about the address. Alongside it, four first-morning failures that all looked like the product being broken: a dev `DATABASE_URL` with no `sslmode=disable` (the driver required TLS, the pool retried the refused handshake, and the API hung on its first query instead of erroring), an Azurite healthcheck asking `nc -z localhost` on a service bound to IPv4 only, a Firebase healthcheck running a `wget` that image does not carry — both of which made `docker compose up --wait` never return — and `tool/api_dev.sh` dying after it had already started serving, because `dart_frog dev` puts the terminal into raw mode and throws from a stream callback when there is no TTY. And the one the whole exercise was for: **signing into the console answered 403 to everybody**. The membership lookup that decides which operator a request belongs to runs on the identity surface, before any tenant is known, because its answer *is* the tenant — and it checked the company was still trading by joining `operators`, a table that surface has no policy on. Row-level security filters; it does not raise. So the join matched nothing, the membership came back null, and every console and back-office request refused a correctly signed-in org owner. Nothing had ever caught it: the API suites build a `TenantScope` directly, the schema guarantees all assert what a surface *cannot* reach, and the only way to meet it is to sign in for real. It is now one bit through one `SECURITY DEFINER` function rather than a policy on `operators` — "the identity surface may read every operator row" is a real widening of the tenancy boundary to answer a yes/no question — with an executed guarantee that holds both halves at once: an org owner resolves to their operator, and `SELECT count(*) FROM operators` on that surface is still zero. Two Postgres tests cover the path, including staff of a suspended company getting no scope. Also reconciled `.env.example` against what the code actually reads: seven variables nothing has ever read are gone (a ticket signing key the issuer ignores, a maps key, a payments gateway switch, an admin URL, an Azurite connection string), and the four `STORAGE__*` the object store needs are present — so uploading a logo now stores it in Azurite and hands back a URL that fetches, where before it answered 503 and the picker was decorative
- ✅ **A running API can be told apart from a working one.** "Has the SMTP started?" turned out not to be about SMTP at all. With no `DATABASE_URL` the composition falls back to fakes — deliberately, so a fresh clone runs — and it then answers 200, serves invented departures and writes the sign-in code to its own stdout. A launch configuration whose `envFile` did not apply produces exactly that, and every symptom points somewhere else: the console signs in, the search returns coaches, and the mail catcher stays empty for a reason nobody can see. The processes now **find `infra/dev/.env` themselves** when `DATABASE_URL` is unset, so there is one answer to "how do I set the local environment" and it does not depend on what started the program — a shell script, a launch configuration, `dart run`, a cron trigger. A variable already in the environment always wins; a set `DATABASE_URL` skips the search entirely; a deployed container never sees it, because the image carries `services/api` and not `infra/dev`. And **`/health` now says what it is wired to** — `{"data":"postgres","mail":"smtp","storage":"objects"}` — which *kind* of thing is connected, never a host, an account or a key. The off switch is not hypothetical: `tool/smoke_api.sh` exercises the fakes composition on purpose in a working tree with a real `.env` beside it, and it was that suite going red that proved the need for one. 13 tests, 2 smoke checks
- ✅ **The vitrine gets an address.** An operator could choose a colour, a header pattern, a logo and a cover, press save on a screen called *votre vitrine*, and have nowhere to send anybody: `GET /public/v1/operators/{code}` had answered with all of it since the vitrine shipped and nothing had ever rendered it. `blt.cg/o/<code>` is that window. **Rendered whole on the server, unlike the follower page**, and the two only look like the same problem: a follower has already opened a link somebody sent them, while this page exists *to be shared*, and the reader who matters most is the WhatsApp crawler building the preview card — which runs no JavaScript. A page that painted itself after a fetch would arrive in a group chat as a bare URL with no name, no cover and no reason to tap, which is the whole job. So the company's name, its own headline and tagline if it wrote them, its lines, its fares and its Open Graph card all come down in the first response. **Indexable**, which is the exact opposite of the follower page and for the exact opposite reason: that one is about a person's journey and carries `noindex`; a shop window search engines cannot see is a shop window facing a wall. The company's eight curated hues and three header patterns are drawn in CSS rather than shipped as files, so the poster and the app agree and nothing has to load first; **what does not move is the structure**, because a storefront an operator could lay out freely becomes a page where the fare is in eight-point grey. A code nobody issued and a company that has stopped selling render the same page, since the reader cannot act on the difference and telling them would let anybody walk the code space to find out who we have suspended. Every line links into the search already filled in — somebody who tapped *Brazzaville → Dolisie* has said where they are going. What that link lands on is the honest gap: `/` is still the JSON stub. 20 server-rendering tests, 11 smoke checks over real HTTP
- ✅ **Search pagination** — the hard `LIMIT 100` is gone, and with it the quiet failure it was hiding: a hundred-and-first departure on a busy road simply did not exist, and nothing anywhere said so. The list is now answered a page at a time with a **keyset cursor**, not an offset — departures are created, cancelled and sold out while somebody is scrolling on a connection where a page takes a moment, and `OFFSET 20` over a list that gained a row repeats one coach and loses another. The cursor names the last row it handed over: **when it leaves and which one it was**, because two companies scheduling the 06:00 on the same road is the ordinary case here and an instant alone would swallow one of them. The same pair is the `ORDER BY`, so the ordering the cursor assumes and the ordering the query produces are one thing. It is **opaque and server-minted** — the moment a handset can build one, the ordering becomes a contract nobody can change — and a cursor nobody minted is **refused rather than answered with page one**, since a client that silently gets the first page back scrolls forever and never finds out. The server asks the database for one row more than a page, which is the whole answer to "is there another?" for the cost of a row rather than a second `COUNT(*)` over the same joins. The app asks for the next page **when the foot of the list is built**, which is exactly when it is about to come into view, and a page that fails to arrive **keeps the rows already on screen** — losing a search to one dropped packet is the failure this market punishes. The count in the header says "et plus" until the list is complete, because a number that grows while somebody reads it was wrong when they read it
- ✅ **The operator's colour reaches the ticket, and the link.** `operators.accent_hue` has carried a closed set of eight since migration 0001 — a CHECK constraint rather than a colour picker, because a free picker guarantees that some company eventually chooses a yellow nobody can read in direct sun. The search results have drawn the left rule in it for months. The **ticket** — the one screen a passenger holds up to another human being, and the one a conductor recognises by livery from further away than they can read a name — arrived in the house green every time, because `BookingDto` carried the operator's name and not their hue. It carries both now: one selected column on a join that was already there, one nullable field on the trip summary, one on the DTO, and the lookup at the top of the ticket. The offline vault keeps the whole JSON blob, so a ticket saved for a flight-mode morning keeps its colour too. **Null stays null rather than defaulting to `foret`** — in dark mode the house green is not the green the rest of the screen is drawn in, so an operator who never opened their vitrine gets the running theme's brand and not a fixed colour; `AccentHue.tryByName` says that once, and the results screen's private copy of the same lookup is gone. The **boarding pass a traveller sends to whoever is meeting them** had no colour at all, which is the same recognition problem on the surface most likely to be opened on somebody else's phone among a dozen tabs: it now has the band, the torn edge and the button in the company's hue. That took a migration, because what the holder of a link may know is decided in the function and not in a handler (ADR-0026) and Postgres will not change a return type in place — the hue is already public on every storefront and every search row, so a link holder learns nothing they could not read on the operator's own page. The **band is dropped for print**: a station's printer is not owed a cartridge of somebody's brand. The hue is **looked up and never interpolated**, tested with a closing brace in the value, because a stylesheet is where a stray one ends a rule and starts somebody else's. The server's copy of the eight hex values — it renders CSS and cannot import a Flutter `Color` — now lives in one file both pages read, and `bel_design` asserts the exact values so a ninth hue cannot be added on one side of that line alone. The freshness ring under the QR stays house-coloured on purpose: it means *this app is computing a live code*, which is our guarantee and not the operator's decoration. 3 traveller · 4 server-rendering · 2 design tests, and 45 migrations re-executed against a throwaway Postgres with every guarantee still holding
- ✅ **The console gets looked at.** The traveller app had a render harness and the server-rendered pages had one; the console — the screen an operator sits in front of all day — had neither, and every one of its eleven screens had been written blind and reviewed by reading its source. Building one took extracting the 750-line scripted gateway out of the widget test so both suites could share it, and it found three things in the first shot. **The day's summary strip stopped wherever its last figure did** — a `Wrap` sizes to its content and a `Card` sizes to its child — so a box floated over a list of full-width rows and read as unfinished rather than as the summary of them. **Every row's actions left the right third of the card empty**: the buttons sat in a `Flexible`, which is loose, so the `Wrap` shrank to fit them, `WrapAlignment.end` had no slack to align inside, and the unused half of the allotment stayed as dead space on every row on the screen. And the one that is not cosmetic: **a cancelled departure was drawn exactly like a running one.** The board query has no status filter, so cancelled coaches are on that list; the DTO carried `status` all the way to the widget, which dropped it. It now carries a red chip, `scheduled` says nothing at all — a label on every row is a label nobody reads — and two tests hold both halves. Alongside it, the headings: `KSectionHeader` existed and only one screen used it, so the fleet, the network and the timetable now head their lists with it, counts and all, and the network's loose help line became the subtitle it always was. **And a drift between the two back-office surfaces is closed**: the console set its page titles in the body face at 21 and the back office set the same thing in the serif at 26, for the same job, on the same design system. `KPageHeader` is that job — the display face, because a page title is the one place per screen it belongs, and a console whose every heading is set in the same face as its table rows is a console where nothing stands out, which is the complaint this whole pass exists to answer. Nine screens across the console and the back office are on it. The back office got the same harness immediately after — five shots, light and dark, and nothing was wrong with it, which is worth saying rather than quietly moving on. 12 shots · 2 console tests · 4 design tests
- ✅ **The one hue that never carried white.** Looking at the console's vitrine preview turned up a divergence — the public storefront has drawn a landscape behind an operator's name since the artwork push, and the preview beside it, the thing an operator uses to decide what their page looks like, drew a slab of flat colour. Putting the same drawing in `KBrandHeader` turned up something older and worse. The eight curated hues are documented as *chosen so white carries on all of them*, and the test that guarded it asked for **3.0:1 — the WCAG threshold for large text**. `laterite` is a light ochre at 3.17:1. It passed. The route name across a ticket band is large text and was fine; the **date under it and the operator's name under that are not**, and those are the lines somebody reads at a coach door with a conductor waiting. Every ticket sold by an operator who picked that hue has been below the line for small copy since the day the set shipped. **A hue now names its own ink**: seven keep white, `laterite` takes the dark ink at 5.57:1, and the test asks for 4.5 against whichever was chosen. `KTicketHeader` takes the hue rather than its colour, because deriving one from the other by reverse lookup is a table that quietly answers `foret` the day somebody passes a colour outside the set. **And the drawing does not go behind the text**: turning its opacity down does not fix that, which is arithmetic rather than taste — `brique` carries its ink at 5.09:1 and is under the line once its ground is lightened by a fifteenth, fainter than a drawing anybody would keep. The company's flat colour is laid back over the left of the band and the landscape fills what nothing is written on. The silhouette palette paints the drawing's own background rectangle in the accent so it disappears into the header, and its transparency comes from an opacity wrapper because the substitution writes six-digit hex and would arrive opaque. 8 contrast · 2 component · 1 traveller shot
- ✅ **The address on the poster answers in words.** Three shipped call-to-action buttons — the storefront's *voir les départs*, the follower page's, and the boarding pass's — pointed at `/`, and `/` answered `{"service":"billetenligne","status":"ok"}`. That is a sentence written for a monitoring probe and read by somebody standing at a bus station who has just scanned the side of a coach. **The reader is decided by `Accept`, not by a second URL**: a landing page parked on `/home` is an address nobody prints on a poster, and moving the JSON to `/api` breaks every probe already pointed at the root, so browsers get the page and `curl` and every uptime checker keep the stub they have always had. `/health` is untouched and remains the endpoint that actually knows whether the database answers. **It is not a booking portal and never becomes one** — a traveller web portal is on the list of things this product deliberately does not build, because the seat map, the offline ticket and the rotating QR are the reasons the app exists and none of them survives being a web page. So the page says what BilletEnLigne is, where to get the app, and stops. **It answers the journey it was asked about**: the storefront's route links have carried `?from=&to=` since the vitrine shipped, and somebody who tapped *Brazzaville → Dolisie* has already said where they are going — both halves or neither, because *Brazzaville →* reads as having lost the thread. **And it shows no store button that goes nowhere**: there is no listing, so the page says the app is not out yet in as many words and the buttons appear only once a deployment has been given a URL (`BEL__PLAYSTOREURL` / `BEL__APPSTOREURL`), the same bargain the app-link claim files next door already make. The hero carries the `journey` drawing as a silhouette, inline, because the reader is on a phone on 2G and half of them arrived from a link in WhatsApp. 13 page tests · 10 smoke checks over a real socket · 4 shots
- ✅ **The signing seed was a literal in the source.** ADR-0007 says the ticket key lives only in a KMS and never on a device; ADR-0020 gives local development a fixed seed so yesterday's screenshot still scans. The adapter carried the fixed seed, its comment said swapping one line was the whole change, and **nothing swapped it** — `Services.resolve` handed the development signer to the Postgres composition as readily as to the fakes. A deployment would have signed real tickets with 32 bytes printed in a public repository: anybody who could read the source could have minted a ticket for any seat on any coach, and the scanner would have gone green on it in front of the conductor. The seed is now configuration (`TICKETS__SIGNINGSEED`, 32 bytes of base64) and **its absence is not silently survivable** — a process talking to a real database refuses to start without one. A seed of the wrong length is a refusal rather than a truncation, because a value somebody pasted half of is a key nobody can reproduce and every ticket signed with it stops verifying the day it is corrected. **The development seed is refused by value**: it is in the source, so copying it into a secret manager is the same accident wearing a costume, and naming it is the only way to catch it. The one way back to it is `BEL__ENV=development`, which `infra/dev/.env` sets and no deployment does — **production is the default**, because the variable that has to be set to make a process strict is the variable somebody forgets on the day it matters. The demo seeder signs with the same resolution rather than the fixed seed unconditionally, so a seeded world whose tickets do not scan is a failure that cannot happen at the coach door. The KMS is still not built and ADR-0007 now says so in as many words; the wire format has carried a key id since the first ticket, so rotation will not need the app to change. 11 config tests · the integration suite now runs on a configured seed rather than the fallback
- ✅ **Something to deploy: two images, and one migration runner.** Everything this system does has been reachable only from a working tree with a Dart SDK in it. Now there are two images — `bel-api` at 23 MB and `bel-worker` at 31 MB, both `FROM scratch` with a compiled executable and no SDK, no shell and no package manager, which is one fewer thing to patch on a process that holds a ticket signing key. Three things are copied in as data rather than compiled: the translation catalog, because the server renders prose on four surfaces; `config/markets.yaml`, because it is the one file a market switch touches; and the migrations, because a schema step whose SQL is invisible is a schema step nobody can review. **The migration runner is a program now, not a shell script.** `tool/migrate.sh` applied migrations with `docker compose exec psql`, which is the right tool for a laptop and no tool at all for a cluster — there is no compose file there, no `psql`, and the database is reachable only over a socket. The ledger rules moved into `bel_worker/src/migrations.dart` and **the shell script now calls it**, because the alternative was a second implementation that eventually disagrees about which files count as applied. It found two things. A refusal used to **create the ledger before deciding to refuse**, so the second attempt looked like a fresh database and would have run `0001` over a live schema; nothing is written before that decision now. And `0044` carried a `\set ON_ERROR_STOP on` — a psql meta-command, which does nothing the scripts had not already said on the command line and stopped the runner dead. `check.sh` now refuses any migration containing one, because a file that works under psql and fails in the cluster is the worst place to find that out. **Building is not the check**: a Dockerfile that compiles and produces an image which exits on its first request is a green build and a broken deploy, so `tool/images.sh` starts both — the API answers `/health` and renders French from the catalog copied into it, and the runner applies all forty-five migrations to a throwaway Postgres over a socket. CI runs it on every push. 7 integration tests · 2 images · 1 CI job
- ✅ **The console and the back office become images too.** Two web bundles behind unprivileged nginx, and three things worth writing down. **They are environment-specific, and that is not a mistake**: the API URL, the Firebase project and its web key are `String.fromEnvironment` in Dart — compile-time, because a Flutter web app has no environment to read at runtime — so the value is baked in and an image built for staging is a staging image. The alternative is a bundle that fetches a config file before it can do anything, which is one more request on the critical path of a login. **A build with no Firebase key and no emulator is refused**, because the console's config defaults to `localhost:9099` — right for `flutter run -d chrome`, catastrophic in a deployment, where it ships a bundle pointing at an emulator that is not there and every sign-in fails with a network error rather than a message. And **the nginx config sets every header once, at the server level**: nginx inherits `add_header` only into blocks that declare none of their own, and because `try_files` rewrites every deep link to `/index.html`, the first version of the file dropped the entire security set — no CSP, no frame refusal — on every route but the root. The cache policy is a `map` now. Each image is started and asked for a deep link, because 404 on a refresh is the default answer nginx gives a single-page app and the commonest thing anybody does to one. **What this found and did not fix: the API sends no CORS headers at all**, so a console served from any origin but the API's own cannot call it — which is every deployment, and `flutter run -d chrome` too. Named here, closed next. 2 images · 3 checks each
- ✅ **The console could not have called the API from a browser.** There was no `Access-Control-Allow-Origin` header anywhere in this tree. A bundle on `console.blt.cg` calling an API on `blt.cg` is blocked by the browser before the request leaves the tab, and so is `flutter run -d chrome` on `localhost:5000` calling `localhost:8080` — which is the local development loop as well as every deployment. **It was invisible to everything that tests this API**: unit tests build their own requests, the Dart client is not a browser, and the handset apps and the scanner send no `Origin` at all. Only a browser enforces this, and nothing here was one. **An allow-list, never a wildcard** (`BEL__WEBORIGINS`, empty by default, and empty means nobody): every interesting route on this API is behind a bearer token, and `*` invites any page on the internet to try one. An allowed origin is echoed back **verbatim**, because a browser compares the header to its own origin character for character, and nothing is derived from the request but the lookup key — which is what makes a reflected-origin bug impossible here rather than merely absent. **Outermost of all the middleware**, for two reasons that are both failure modes: a preflight is an `OPTIONS` to a route with no `OPTIONS` handler, so answering it after routing makes it a 405 with no CORS headers, which a browser reports as an opaque CORS failure; and a 401 needs the header as much as a 200 does, or the browser hides the response and the console says *network error* for what was *your session expired*. Credentials are deliberately **not** allowed — this API authenticates with a bearer token in a header, never a cookie — and the trace id, the idempotency-replay flag and a statement's filename are exposed, because a page that cannot read the trace id cannot report the bug. 10 unit tests · 9 smoke checks over a real socket
- ✅ **A cluster to put it in, and two bugs the manifests found.** `infra/k8s/` is a kustomization for GKE — three hostnames on one load balancer, a Google-managed certificate, a plain-HTTP redirect at the edge, two API replicas rolling with `maxUnavailable: 0`, the two web apps behind unprivileged nginx, KEDA ScaledJobs for the passes, and a migration Job that runs to completion **before** anything that reads the schema is rolled. The console is on its own hostname rather than a path, and that is forced rather than chosen: `/console` and `/admin` are already API route surfaces, so mounting the web apps there would collide with the endpoints they call. **`/ready` is a new endpoint and a different question from `/health`**: `/health` touches no dependency because a liveness probe that queried Postgres would restart the whole fleet during a blip, turning a thirty-second outage into a fleet-wide cold start; `/ready` takes one round trip, so a pod that cannot reach the database leaves the load balancer and returns when the database does, with nothing restarted. **The worker takes several pass names now rather than one**, because the passes do not share a cadence: `payments` wants five minutes and `reliability`, which recomputes on-time rates over every departure a company has run, wants once a night — and with one name per invocation the only deployable shape was *everything, often*. Writing the schedules found two things. **The worker's exit code never worked**: Dart ignores a value returned from `main`, so the file that says *non-zero if any pass threw* has exited 0 for its whole life, and a ScaledJob would have recorded every failed drain as a success. And **one pass was on no schedule at all** — `alerts-expired`, found by the new refusal to accept a pass name that does not exist, and now guarded by a CI check that compares the worker's own list against the ScaledJobs'. 3 smoke checks · 3 CI checks · 15 manifests
- ✅ **The infrastructure, written down.** `terraform/` describes both halves: a VPC with private GKE nodes behind Cloud NAT, Artifact Registry, Cloud SQL for Postgres on a private address with backups and point-in-time recovery, and the static IP the posters point at — plus, on Azure, the Blob container that holds KYB documents and ticket PDFs and the Communication Services resource that sends the email. **The split across two clouds is a decision, not history**: the cluster and the database are where the account and the billing are; the messages and the object store are where the sender identity and a signing implementation that CI already exercises against Azurite are. **Cloud NAT is load-bearing** rather than boilerplate — private nodes have no external address, and this system has to reach an Azure storage account, an Azure Communication Services endpoint and four mobile-money hosts; without it every adapter times out and it looks like a broken adapter rather than a missing route. **Terraform deliberately does not create `bel_api`**, and that is the sharpest thing in the slice: the role is created by migration 0005 NOLOGIN and NOINHERIT, and a Cloud SQL user of that name made first would be an *inheriting* role that the migration's `IF NOT EXISTS` would politely skip — every request then running with the union of the public, tenant and platform privileges, which is row-level security switched off without a line of code changing. `verify.sql` now asserts the role exists, does not inherit, and that no surface role can bypass RLS. **KEDA has no Terraform field**, so it is a `gcloud` step in the README and a comment in the module: a ScaledJob on a cluster without it is accepted as an unknown kind and never runs, which is the quietest failure in the whole deployment. Everything validates under `terraform validate` in CI. **Nothing has been applied** — no cluster exists, and every number in these files is a starting guess rather than something a load test produced. 4 modules · 1 schema guarantee · 1 CI job
- ✅ **The release build, and five things only it could find.** Every check in this repository until now ran on a debug build or on the Dart VM, and a release APK differs from both in ways nothing was exercising: a different manifest merge, a different signing certificate, and the `--dart-define`s compiled in and unchangeable. **The traveller app could not open a socket.** The Flutter template declares `INTERNET` in `src/debug` and `src/profile` with a comment about hot reload and nowhere else, so every screen worked all the way through development and the release build failed every request the way a dead network fails — on a handset with four bars, first seen by the person holding the sideloaded file. It is now in the main manifest of both mobile apps, and the check that catches it is the first thing `tool/release_android.sh` does to an artifact. **Release builds were signed with the debug key**, as the template ships them: a certificate whose password is `android` and which is on every machine that has ever built an Android app, which means anybody at all can publish an update to a sideloaded install. With no `key.properties` the release now comes out unsigned and will not install — the loud failure rather than the quiet one — and the script refuses any artifact whose certificate says `CN=Android Debug`. **The launcher said `bel_traveller`.** **The deep-link host was a constant**, so a staging build would have claimed `blt.cg` and been refused by an assetlinks file it does not serve, which presents as links that stopped working with nothing in any log; it is a manifest placeholder taken from the API address. **And `--split-per-abi` does not ship the build number you gave it**: Flutter adds a thousand per architecture, so with the commit count as the base, commit 1149's armeabi APK and commit 149's arm64 APK are both versionCode 2149 — a Play rejection, months out, with nothing wrong in the tree. The base is multiplied to leave room. A release APK also runs the demo gateways when it was built without `BEL_API_URL`: an invented search, a seat map, a payment code, and somebody at a yard at half past five with a ticket no conductor's scanner has ever seen. That build now says so on its first frame. 6 checks per artifact · 3 ABIs · 2 apps.
- ✅ **The apps get their faces.** Four marks had been SVGs in `brand/icons/` since the brand was drawn, and all four apps shipped Flutter's default beachball — which is what an operator's customers would have been asked to find on a home screen full of other apps. They are **rasterised by the app's own renderer**: `flutter_svg` into a `RepaintBoundary`, so the icon on the launcher is by construction the icon the app draws, and there is no second rasteriser to install and no second output to reconcile. The adaptive icon needed the mark's **background rectangle taken out of the foreground** — leave it in and a launcher cropping to a circle crops a square that was already there — and its fill becomes the background layer. The iOS set is written as PNG colour type 2 from raw pixels, because `toByteData` always writes RGBA and **Apple refuses an icon with an alpha channel**: a rejection that arrives at submission, months after the file was made, about a transparency nobody put there. Freshness is a **tolerance rather than a byte match** — every file is re-rendered on an ordinary `flutter test` and compared, with a mean-error threshold, because Skia's antialiasing moves by a shade between engine versions and a byte match would turn every Flutter upgrade into a red build about an icon nobody touched. 62 PNGs · 2 XML resources · 4 tests · no new dependency.
- ✅ **The iOS project, read the way the APK was.** Every Android defect in the release slice was found by making the artifact and opening it; iOS cannot be built here, so the same class of defect would have waited for the first day somebody had a Mac and an Apple team. The project files are read as the text they are — sixteen checks, five of which failed. **The boarding scanner had no `NSCameraUsageDescription`**: iOS terminates the process the instant the camera opens without one, with no dialog, no error and no log line — the app simply disappears at the coach door, on the first ticket of the first morning, and it is that app's entire job. **The entitlements file was attached to no target** — `applinks:blt.cg` has been declared since the ticket-link slice and `CODE_SIGN_ENTITLEMENTS` appeared in neither project, so the app's half of a handshake the API has been serving all along was made nowhere, and the symptom is a ticket link opening Safari. **The apps were called `Bel Traveller` and `Bel Scanner`**, **neither said it speaks French**, and **the export-compliance question was unanswered**, which means it is asked on every upload and answered by whoever is in a hurry. 16 checks · 1 CI step · nothing compiled.

---

## Phase 5 — Adjacent markets

Each is a deliberate decision with a gate, not an assumption.

| Bet | Why | Gate |
|---|---|---|
| **Air** (ADR-0017) | Same manual problem, higher value per booking. The seat map already renders a two-class cabin | One design-partner carrier **and** the compliance package: ID capture, check-in, baggage, exit rows |
| **Colis / parcels** | Already an informal business on the same coaches, high margin | The anchor operator asks for it |
| **DRC** | Larger market; config plus one PSP adapter. `market_test.dart` already stands up a full DRC market in test code | Congo-Brazzaville unit economics proven |
| **River ferry** | Manual, high volume, structurally identical to bus | Opportunistic |

---

## What we are deliberately not building

Loyalty programmes · in-app chat · dynamic pricing · ETA machine learning · a traveller web booking portal · multi-leg itineraries · ride-hailing.

Each is reasonable, and each would dilute the one thing that has to be excellent first: **a traveller in Brazzaville can buy a seat with mobile money and board without an argument.**

---

## The risks that decide whether this works

| Risk | Leading indicator | Mitigation | Status |
|---|---|---|---|
| Telco merchant onboarding stalls | Weeks since application, no credentials | Phase 1 ships without it; three rails pursued in parallel | **Unmitigated — no application submitted** |
| Anchor operator does not sign | No LOI | The cash-only console is free and useful on its own; lead with that | **Unmitigated — no LOI** |
| Mobile money success below 85% | Per-rail, per-hour dashboard | Rail fallback plus an aggregator escape hatch (ADR-0006) | **Both adapters built and tested against a fake that reproduces every terminal state. Unmeasured until real traffic** |
| An operator mistypes their collection number | — | Saved unverified; no rail is offered until somebody has checked it | Mobile money has no chargeback, so this is the one typo in the product that cannot be undone |
| Travellers do not trust prepayment | Funnel drop at the payment screen | SMS receipts, honest scarcity, visible refund policy, cash retained | Design done; unproven |
| Disruption overwhelms support | Support tickets per disruption | IRROPS is operator self-service and P0 in Phase 2 | Built — declaration, rescue coach, rebooking wave, protection movement, the open call to the whole road, and the passenger's own choice |
| **Nobody has used this on a real network** | — | Phase 3 manual smoke | Everything so far is an emulator on a fast connection |
| **No operator can configure anything without us** | Time from "yes" to first departure on sale | The console app | **Closed.** An operator configures a fleet and publishes a timetable in a browser |
| Console auth is single-factor | — | TOTP before refunds and payouts exist (slice 3) | **Closed.** The endpoints ADR-0013 protects — refunds and payouts — are built, and TOTP went in ahead of them: mandatory on both back-office surfaces, shared by the console and the back office through `bel_backoffice`, with staff who hold no factor landing on the enrolment screen and nowhere else. What is still narrower than ADR-0013 is the *first* factor: an emailed code rather than a password |
| **Sign-in email does not arrive** | Delivery rate per hour, from day one of the pilot | Phone as a second channel — which is why it is plumbed rather than someday (ADR-0024) | **New. Email is now on the critical path of becoming a customer, and it has no fallback yet** |

The first two are commercial, and they are the ones that decide the outcome. Nothing in the engineering backlog above changes either of them.

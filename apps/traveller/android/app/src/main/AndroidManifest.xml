<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Declared here, not only in the debug manifest.

         The Flutter template puts INTERNET in `src/debug` and `src/profile`
         with a comment about hot reload, and nowhere else — so every screen
         works all the way through development and the **release** APK cannot
         open a socket. It does not crash and it does not say why: every
         request fails the way a dead network fails, on a handset with four
         bars, and the first person to see it is holding the sideloaded build.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:label="BilletEnLigne"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- A ticket link opens the app, and only a ticket link (ADR-0026).
                 `autoVerify` is what makes it silent: without it Android asks
                 "which app?" every time, and a chooser in front of a boarding
                 pass at a coach door is worse than a browser. The pair is
                 checked against /.well-known/assetlinks.json, which this
                 server serves.

                 The follower page at /t/ is deliberately not claimed: it is
                 opened by strangers with no account and no app. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https"
                      android:host="${belLinkHost}"
                      android:pathPrefix="/b/"/>
            </intent-filter>
        </activity>
        <!-- Hands the incoming URL to Flutter as the initial route rather than
             through a plugin channel. One less package, and the path arrives
             before the first frame — which is the difference between opening
             on the ticket and opening on the search screen and jumping. -->
        <meta-data
            android:name="flutter_deeplinking_enabled"
            android:value="true" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
